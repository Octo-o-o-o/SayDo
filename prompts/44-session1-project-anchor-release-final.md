# 场次①项目归属发布终验 44

只读审查当前稳定工作树，不修改文件、不提交、不部署。前序终验 39–43 因主会话按独立
评审持续回修而中止，不作为结论。

核验：

1. 项目归属问句必须是恰好两个完整 alternatives，一侧 full-match 新建分支，另一侧
   full-match 续接分支；句内/相邻跨句疑问证据绑定正确。
2. 成对“要么 A，要么 B”只定向规范化首个要么，后文必须仍有配对要么；以下为 true：
   “要么新建项目，要么沿用旧项目？”、“你要么新建仓库，要么继续之前的？”。
3. 所有既有正例命中；普通陈述、无关问号和嵌套成本/决定问句均不命中。
4. live 使用 provider 完整原文，在截断/分句前阻断；纯对话与工具环成功出口一致。
5. CLOSING owner 占席但 JSON/binary/TTS 全阻断；`/readyz` 使用 transport-aware helper。
6. canonical、实现、测试一致；审计 37 其他关闭项不回归。运行允许的门禁。

先给可放行/仍需阻断和 A/B/C。发现须给 file:line、复现、后果、最小修复；无 A/B 时明确
A=0、B=0。仅 stdout。
