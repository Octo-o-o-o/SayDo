# 场次①项目归属发布终验 42

你是最终对抗性评审者。只读审查当前工作树，不修改文件、不提交、不部署。审计 38 结论为
A=0、B=2、C=0；审计 39–41 均因主会话按并行独立评审继续回修而中止，不作为结论。本轮
只对启动时的稳定快照终验。

必须核验：

1. Brain 归属问句闸按局部 choice window 判定：疑问证据绑定同一句 connector 后或相邻
   跨句窗口；无标点询问框架完整匹配；整句必须在第二个选择分支本身结束。
2. 正例须命中：新仓库/旧仓库、另建代码库/原先、从头/老库、反向顺序、跨句
   “这是新需求。还是回到之前那个项目？”，以及“你要新建仓库还是沿用旧仓库”。
3. 以下嵌套陈述/问句必须不命中：
   - 决定新建仓库还是沿用旧仓库之前，先看成本够不够？
   - 新建仓库还是沿用旧仓库的决定，明天能给我吗？
   - 决定新建仓库还是沿用旧仓库之前，预算够吗？
   - 成本够吗？决定新建仓库还是沿用旧仓库之前，先看预算。
4. live 输出闸读取 provider 完整原文而非最多六句口播列表；问题跨句或位于截断范围之后
   仍整体阻断。检查纯对话与工具环成功出口。
5. VoiceHub owner 进入 `CLOSING` 后仍占唯一席位，但 JSON、二进制、TTS 下行均停止；
   `/readyz` 生产接线直接使用 transport-aware `assessRuntimeReadiness`。
6. canonical 与实现、测试一致，审计 37 其他已关闭项不回归。运行只读环境允许的
   typecheck、lint、聚焦测试、ruff、`git diff --check`。

输出先给“可放行/仍需阻断”和 A/B/C 数量。每条发现必须给 file:line、复现输入或时序、
后果和最小修复。没有 A/B 时明确写 `A=0、B=0`。报告只输出到 stdout。
