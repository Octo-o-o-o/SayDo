# PG-01A FG-1 fresh readback 1

你是全新零上下文、只读的 Codex reviewer。只回答当前 FG-1 candidate 是否满足固定 handoff；不得修改产品代码、测试、cycle-state、handoff 或 P2 ledger，不得派 agent，也不得读取 Grok 实施 transcript、自述或 `fg1-repair-1.log`。

## 固定身份

- repository：`<validation-worktree>`
- task：`project-gap-closure`
- stage：`PG-01A/FG-1`
- cycle：`owner-night-fg1-recovery-20260901`
- review ordinal：1
- review scope：step
- HEAD：`2b5517d322f062297d25806b02c4106e2ecc60e8`
- `git-diff-v1`：`d324b59bce25b1c3eedb0110c77e520f160f57bc44c2ad3e6b51552ac26444e8`
- handoff：`<worktree>/docs/plan/IMPL-PROMPT-PG-01A-FG1.md`
- frozen policy：`<worktree>/docs/plan/project-gap-closure/policy.frozen.json`
- cycle-state：`<worktree>/docs/plan/project-gap-closure-cycle.json`
- focused receipt：`<worktree>/logs/pg01a-night-20260901.V1D8er/fg1-focused-1.receipt.json`
- pre/post scope：同日志目录的 `pre-scope-fg1.json`、`post-scope-fg1.json`
- P2 ledger：`<worktree>/docs/plan/2026-08-28-project-gap-closure-program-DEFERRED-P2.md`

先独立运行 `git log -1`、candidate fingerprint、status/diff，并完整读取 handoff。不要把旧 ordinal 4 的公开声明 GREEN 当成本 candidate 的 FG-1 评审；也不要重审公开声明、scanner 或项目历史。

## 当前变化与必须核验的问题

起始 validation 有 15 项既存 PG-01A dirty 路径。本次 pre/post 全量快照显示三路径 may-change 之外 2029 项摘要均为 `7661120cfe7abc30e6d705d1a3a7b0f73d7caca3c7f24fbf6dbf0f538d91ff7e`；新增产品 delta 只有 `packages/daemon/test/tier1-executor.test.ts`，8 行变化。不要据此直接判 GREEN，须自行读代码与相邻 lifecycle 实现。

逐项判断：

1. callback 内是否仍直接证明 `ownershipEstablished` 发生时 durable `agent-owner.json` 已存在。
2. 删除 callback 返回后的立即 exists 断言，是否正确消除了 publication 后 settle/clear 合法并发造成的错误同步假设，而非弱化真正安全不变量。
3. 新增等待是否同时证明 executor 已无 active run 且 owner marker 已清理；是否会产生新竞态、假绿、泄漏或 timeout 掩盖。
4. production `establishAgentOwnership` 与 `clearAgentOwnershipAfterDurable` 的 happens-before 是否支持上述测试语义；若实际 production 有缺陷，test-only delta 不得判 GREEN。
5. 是否存在 skip/only、timeout 扩大、sleep、重试循环、断言放宽、编码绕检查或范围逃逸。
6. focused receipt 六项是否真实绑定同一 candidate；必要时可读 focused 原始日志，但不要把 runner exit 0 当成产品结论。只读沙箱若阻止测试重跑，如实说明，并以已核 hash 日志与独立静态取证分别表述。

未在新 candidate 上运行完整 `just ci`，这符合门禁顺序；不要因缺 full gate 判 RED，也不得宣称 full gate 已通过。

## 报告要求

输出完整 readback，含身份、FG1-1 至 FG1-5 对账、质量发现、focused 证据、P2 ledger delta 与唯一一个 `review-manifest`。GREEN 仅在没有 P0/P1 或验收失败时成立；P2 只给 delta，不修改 ledger。manifest 必须绑定上述 HEAD/fingerprint/ordinal，`focused_gates` 只含 `name`、`exit_code`、`summary`，不得含可执行命令字段。RED blocker 使用稳定 ID `FG-1` 或新的明确 ID，并给出 `root_cause_id` 证据时仍保持可机械判定；不要因这是 ordinal 1 之外的旧项目历史自行停止。
