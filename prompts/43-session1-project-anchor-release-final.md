# 场次①项目归属发布终验 43

只读审查当前稳定工作树，不修改文件、不提交、不部署。审计 38 为 A=0、B=2、C=0；
39–42 因主会话按独立评审持续回修而中止，均不作为结论。

重点终验：

1. 项目归属问句 choice 必须恰好两个 alternatives，两侧分别完整匹配
   `isNewProjectBranch` / `isExistingProjectBranch` 且一新一旧；疑问证据只绑定句内或
   connector 开头的相邻跨句窗口。
2. 正例包括新仓库/旧仓库、另建代码库/原先、从头/老库、反向顺序、跨句、
   无问号明确询问框架，以及“沿用原先的/接着之前的”。
3. 嵌套陈述/问句必须不命中，包括：
   - 决定新建仓库还是沿用旧仓库之前，先确认这个？
   - 决定新建仓库还是沿用旧仓库之前，我们还要继续？
   - 新建仓库还是沿用旧仓库的决定，明天能给我吗？
4. live 使用 provider 完整原文、在截断和分句前阻断；纯对话与工具环成功出口一致。
5. CLOSING owner 仍占席但 JSON/binary/TTS 全阻断；`/readyz` 直接使用
   transport-aware `assessRuntimeReadiness`。
6. canonical、实现、测试一致；审计 37 其他关闭项不回归。运行允许的门禁。

先给“可放行/仍需阻断”和 A/B/C。发现必须给 file:line、复现、后果、最小修复；没有
A/B 时明确写 A=0、B=0。报告仅 stdout。
