# PG-01A C1 · fresh readback 2

你是与实施线零上下文的独立只读 reviewer。使用 /impl-review，task_mode=review_only，review_scope=step；不施工、不派 agent、不调用其他实施或 review CLI、不自行开启新 cycle。只审本文件的候选与验收，不继承任何实施自述。

## 固定对象与输入

- task=project-gap-closure；stage=PG-01A/C1；cycle=owner-recovery-20260831-2；review_ordinal=2。
- 被审目录：<validation-worktree>。
- supervisor 控制文档目录：<worktree>；以下相对输入均在该目录，不要求复制进 clean I。
- P=79211f6fec5c6eb092419c1871e35d8eedc4e3e5；被拒旧 I=004b226db0b9a6fb0347d60dc790c97293d11635；新 I=2b5517d322f062297d25806b02c4106e2ecc60e8；tree=c5cae2f04ee907328b672e3099c865f1c3f83552；git-diff-v1=e42dd19ca752c0ee2dca966fb0e915cbc9fce9375efcd3d45a53f060692f9727。
- 唯一验收合同 docs/plan/IMPL-PROMPT-PG-01A.md，SHA-256=2a74991ec2af1c511490ce330c4c8440d0d77149923be6d9f1e9a7c791a264ad。完整读取，先运行 validate_handoff.py；其施工角色指令仅作为被审合同，不要求你执行。
- 授权 docs/plan/2026-08-28-project-gap-owner-decisions.md 第 6/7/8 节，SHA-256=5e123aa2236ddc8553d55f65244e98cd1a21139fc6857783db0f7993e1a5970b。
- 唯一 ledger docs/plan/2026-08-28-project-gap-closure-program-DEFERRED-P2.md，SHA-256=4a9274b9308a98c55d5152d06daedc8b6760d0fd088cdbc25cabb5a87f4fdae8；只读，P2 仅输出结构化 delta。
- 计数来源 e2e/evidence/project-gap-pg-01a.md，仅取开头 13 行：本 cycle repair=1、rereview=1、C1_review=1 为本次派发已记账；不是已有一次对新 I 的 review。旧 cycle RED 保留，不能重复扣减。不要读取后续实施过程或 CLI transcript。
- 当前机器策略为 2.3，合同已同步；本 cycle 第 8 节钉住的一次修复授权不被新全局上限扩充。你只输出实际 verdict/blockers，禁止因 ordinal=2 自动填 second_red。后续路由由 supervisor 用 validator + cycle-state 决定。

先用真实 git log/status/fingerprint 重新核验 HEAD/tree/P，并核验上述输入 digest。新 I 与旧 I 都在主仓共享对象库的本地 branch ref 上，无需 fetch 或创建引用。clean dirty fingerprint 与旧 clean I 相同是空 diff 的正常结果；身份是 HEAD+fingerprint，不能把旧 I 报告当成新 I 的审查。P..I 共 33 路径，完整核对而非抽样；旧 I..新 I 是本轮恢复 delta。

## 原 blocker 摘要与本次验收

- B1 / P1 / C1-1：现役页面仍有绝对隐私、零费用、任意兼容或固定低时延承诺。要求按 C1-9 全量清查既有 31 个 required roots，中英文条件与正文一致，不仅删报告举例；不以总免责声明掩盖正文。
- B2 / P1 / C1-2：active-claim checker 漏真实中英文变体；旧组合反例仅断言 free，不能证明全支持也被拒。要求免费、全 CLI、API 兼容、固定时延、数据边界各自独立反例；保留独立 root 集合、移除 root 和假 LIVE 反例；合法 conditional/preview 与观察时延不得被误判。
- B3 / P1 / C1-3：Q0 checker 接受伪造 reason、locator_check、required_field_check；旧反例被同时改变 status 掩盖。要求八字段的漂移/删除/错误类型逐一独立被拒，并保留 986 coverage、排序、aggregate、disposition、Git identity 与 clean I producer 限制。

逐项对账 C1-1 至 C1-9，重点亲验三项 blocker 与不可绕过的不变量。C0 是已评审的前置合同，读取 docs/06 第 7 节及 docs/11 第 10.3 节作为形状依据，不重新派 C0 reviewer。原 source/48 authority inputs、dry-run renderer/oracle、运行时与权限不得改变；setupApi 等 TSX/TS 只能改文案/相应断言。
审查新增门禁是否真能拦错误，不把测试名称或执行者自报当证据；可做纯函数、内存中负例。所有 blocker 须给可复现证据、repo-relative file:line、acceptance_item、in_scope、needs_owner_decision。P2 不提升为 blocker 以触发额外修复。

## 定向证据与门禁顺序

同一新 clean I 的完整 16 项 focused 原始记录位于控制目录 logs/pg01a-recovery2-20260831.i5h14e/focused-on-i.log：24122 bytes，SHA-256=c37af2d284f60c61ffcc84612ca35c93c1ea15fd3d010ce3bb839dda40d4d816；相邻 focused-on-i.summary.json 是 runner 结果。核验摘要、原始 argv/exit 与 tested_implementation_sha/tree，不仅相信该段描述。日志只含门禁事实，不含实施推理。
先实际运行覆盖 console/src 的 pnpm --filter @saydo/console typecheck，再按合同挑选本轮受影响 focused checks；不运行完整 just ci。只读 sandbox 可能拒绝临时 fixture 或 Vite cache 写入：如发生 EPERM，原样记录该命令未成功，避免重复尝试、放宽 sandbox 或把它记绿。可以读取并明确归属上述同 I 的实际 focused 输出，再用只读代码及内存负例独立核验，不能伪称自己重跑了 fixture。
GREEN 是 C1 语义 readback，不是整个项目/PG-01A 交付通过。just ci、页面渲染、正式 Q0 producer 和 E 尚待 supervisor 在语义 GREEN 后执行；I 中无绑定自身 SHA 的正式 report 是合同要求，不是漏交。真实账号、live AI、connector、发布/设备/签名测试未运行，不得声称已验证全部用户或 AI 服务。

## 只读边界与输出

不修改任何被审文件或控制文档，不创建正式 Q0、不 rebuild、不 commit/push/merge、不访问账号凭据或真实产品服务、不申请扩大权限。允许命令输出由 CLI -o 写到本次唯一 review artifact；必要临时 fixture 不得污染源仓。使用简体中文、零 emoji，以 [ok]/[warn]/[fail] 标记。
报告包含坐标与合同校验、C1 逐项台账、B1/B2/B3 结论、原始命令/输出摘录、P2 ledger delta、未运行清单；引用代码用纯 repo-relative file:line，不用本机绝对路径 Markdown 链接。不得阅读父会话、Grok 会话或恢复提示，不能将实施叙述作为证据。
末尾必须唯一一个 review-manifest 块，严格使用 /impl-review schema。GREEN 必须 blockers=[]、stop_reason=null、focused_gates 全部 exit_code=0；有未成功的尝试在正文明确写清，不能伪装为 pass gate。RED 只输出真实 P0/P1/验收 blocker，通常 stop_reason=null，不能因为本次编号自动填 second_red。focused_gates 仅 name/exit_code/summary，不含 command/run/script 等执行字段。review_ordinal=2，candidate_head 和 diff_fingerprint 必须是你实测确认的新 I 坐标；review_scope=step。不对后续修复、自主加轮或发布作授权。
