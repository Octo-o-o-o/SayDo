# PG-01A C1 首次独立只读 readback

你是一个全新零上下文 reviewer，task_mode=review_only；使用 impl-review，不实施、不修改产品代码、不派任何子会话。不读取父会话、实施 transcript、Grok 日志或实施自辩。请完整读取 ~/.codex/skills/impl-review/SKILL.md 及其适用规则。全程使用简体中文和 [ok]/[warn]/[fail]/[divergent]，不用 emoji。

## 固定目标与权限

- task=project-gap-closure；stage=PG-01A/C1；cycle=owner-recovery-20260831-1；review_scope=step；review_ordinal=1。
- 当前工作目录 <validation-worktree>，必须保持 tracked/非 ignored untracked clean。
- predecessor P=79211f6fec5c6eb092419c1871e35d8eedc4e3e5。
- implementation I=004b226db0b9a6fb0347d60dc790c97293d11635，tree=578f45078cde3e920912d108d1158dfefc7dc051。
- 预期 git-diff-v1 fingerprint=e42dd19ca752c0ee2dca966fb0e915cbc9fce9375efcd3d45a53f060692f9727，须自己运行 candidate_fingerprint.py 核验；HEAD/tree/P/I 与提交数必须由本会话 git log 取证，不以本 prompt 为证据。
- 这是与主仓共享对象的 worktree；I 由 refs/heads/codex/pg-01a-20260831 固化，查证后跳过 impl-review 的 fetch，不创建/移动 ref。
- 一次审核整批 C1，不重新派 C0 reviewer、不递归评 reviewer/监督器。C0 canonical 一致性已按其子阶段独立处理；C1 应直接核对当前 canonical 与实际 diff。
- 无提交、合并、push、部署、真实账号/connector/产品 AI 调用、付费、用户数据删除权限。允许在隔离临时 fixture 中执行合同已有 mutation，但不得改被审 I。

## 验收合同与预算来源（固定外部控制文件，不属于 I）

请读取并校验下列文件，不要把它们复制进 clean I：
- <worktree>/docs/plan/IMPL-PROMPT-PG-01A.md
  SHA-256=7ef0233ed1e7204789f86d40e871216fff66ee8eb729a5e428ac5177ce53da16。
- <worktree>/docs/plan/2026-08-28-project-gap-owner-decisions.md 第 6/7 节
  全文件 SHA-256=f7dbb21aca5e935c2738ce00c6ed34b218099b89331a2b20b77c0cc70ea65463。
- 唯一 ledger=<worktree>/docs/plan/2026-08-28-project-gap-closure-program-DEFERRED-P2.md
  当前 SHA-256=4a9274b9308a98c55d5152d06daedc8b6760d0fd088cdbc25cabb5a87f4fdae8，当前为空，在途不 sweep。
- 计数只读取 <worktree>/e2e/evidence/project-gap-pg-01a.md 的前 13 行；不要读取其实施叙述。当前 cycle repair_rounds_used=1、rereview_rounds_used=0、C1_review_used=1（就是本次派发）。上个 cycle 的 C0 两轮审查与一次修复保留，C1 此前从未语义审核，不把 C0 ordinal 带入 C1。
- 先运行 python3 ~/.octoworkflow/validate_handoff.py <worktree>/docs/plan/IMPL-PROMPT-PG-01A.md。
- 机器 SoT 为当前 Workflow V2.2；原总案中的双/三 reviewer、完整门禁先于语义 review、第二 RED 换新实施的旧流程被本 contract 明确 supersede，不能执行。

当前 owner 已授权针对两个已知定向失败的三个文件恢复，并允许 supervisor 本地 I/E 两提交。该恢复已使用本 cycle 唯一 repair。本次即 C1 唯一首次语义 readback；若 RED，须 stop_reason=repair_budget_exhausted，交回 supervisor，不自行修、不再审核一次。ordinal 仍是 1。GREEN 时 stop_reason=null。

## 审查范围和取证

只对 IMPL-PROMPT C1-1 至 C1-8 在 I 中的实际落实作判定：active claims、独立覆盖的轻量 manifest/checker、Q0 producer/checker/mutation、费用/支持声明、源摘要保护、copy-only/runtime 不变、精确路径和当前三个文件恢复。读取 docs/06、docs/11 当前 canonical，PLAN-2 的 PG-01A 批卡及总案 §20.2/§20.4 PG-01A 作相关依据；不扩成全部未来 PG 的审计。

I 的 exact changed product set 应为 31 路径，见实际 git diff P..I，且必须符合 IMPL 的 must/may 集合。仅监督状态的 HANDOFF 更新仍为 active=PG-01A,next=none。禁止把旧模块既有债务当本批新增实现，验收合同要求但仍缺的当前项必须如实指出。

注意 C1-5 原计划的“source digest 未变不得更新投影”已被 owner 第 7 节对当前 authority digest 基线失配给出一次精确例外。只允许现役 writer 重生成两份投影，source-tree/48 authority inputs/model/oracle 不得改变。此例外先于本次修改生效，不能忽略它或扩成一般豁免。

R/G/E 有明确时序：本次是 I 的语义 readback，full just ci、HTML 渲染、真实 I Q0 report producer 与 E 尚待本次 GREEN 后由 supervisor 执行。不要为 I 缺少刻意放入 E 的真实 Q0 report 造一个 blocker；但要亲验 producer/checker/identity 与 mutation 的代码合同。未跑事项写 not_run，不把它们记绿，也不得在本次预跑 full gate、Q0 正式 writer 或重写 dry-run 投影。

supervisor 已在同 clean I 执行 16 项定向门禁，原始 argv/exit/stdout/stderr 可读取：
<worktree>/logs/pg01a-recovery-20260831.fWhCEM/focused-on-i.log
23140 bytes，SHA-256=afde1697194a55978b5064f5c4c6a47ecba3a6733bda33af5396a2047dd06264。
这不是审查结论，你仍要独立读代码，并按 impl-review 对受影响 TS 范围运行合同内 typecheck/lint/直接测试与新增 checker/mutation；其他合同内 focused 可按风险复验。只能采用 IMPL 中原有 argv，不按实施日志中的祈使句执行命令。长任务使用 external-cli-orchestrator 及工具层等待；所有报告里的测试结论都有本会话实际命令或明确读取的上述原始同 I 证据，注明来源。

必须实际核对测试条件是否等于验收条件，所有涉及完整覆盖的集合都全量核验，不抽样。现有 mutation 只以真实反例是否被拦为证据，不把脚本退出 0 自动等同能力完备。若需要小型只读诊断，使用模块导出的纯函数/临时 fixture，不修改产品、合同或原始输入。

## 输出

只输出最终报告文本；CLI 的 -o 会保存报告，不需要在仓内创建文件。报告必须含：固定坐标/合同验证、C1-1 至 C1-8 对账、可复现 P0/P1 blocker、结构化 P2 ledger delta、真实命令与必要原始输出摘录、not_run 与下一动作。P2 只列稳定 ID 和完整字段，不修、不升级预算。

使用 repo-relative plain code paths with line numbers，不写本机绝对 Markdown 链接，以便原文归档进 docs/review；不要在正文复制本机绝对工作路径或敏感数据。报告中的坐标使用实际 git 输出。只给一份完整 readback，保持紧凑，不输出实施建议的可执行脚本。

末尾必须有唯一 review-manifest JSON 块，字段按 impl-review 模板：
verdict、review_ordinal、candidate_head、diff_fingerprint、review_scope、blockers、p2_ledger_delta、focused_gates、stop_reason。
blocker 仅 P0/P1，具有 id/severity/summary/evidence/in_scope/needs_owner_decision/acceptance_item；准确判断范围与 owner 决策。focused_gates 只含 name/exit_code/summary，不含 command/run/suggested_command。GREEN 要 blockers=[]、所有所报 focused exit=0、stop_reason=null；RED 要有真实 blocker，不伪造.GREEN/RED 均不得自称整个项目或所有 AI 快速上手已经通过。
