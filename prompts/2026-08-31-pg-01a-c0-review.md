# PG-01A/C0 独立 canonical 一致性 readback

你是全新零上下文、只读 reviewer。只回答当前 C0 是否满足其已授权验收合同，不实施、不派其他语义 agent、不调用实施 CLI。本任务 review_scope=step，review_ordinal=1，task=project-gap-closure，stage=PG-01A/C0，cycle=owner-continuation-20260831。

先完整读取 ~/.codex/skills/impl-review/SKILL.md；按其证据纪律输出 readback。当前 Workflow 机器可读 SoT 与验收合同优先于历史文件中多 reviewer 或无限返工叙述。当前合同是 docs/plan/IMPL-PROMPT-PG-01A.md；先运行 validate_handoff.py 核验。若合同或现场身份不符，停止并给结构化 blocker。

## 范围和权限

- 只评 C0-1 至 C0-6，以及 C0 三项 focused gate。C1 尚未释放、不得把 C1 未实现判成 C0 红灯。
- 需要读取：当前 AGENTS.md；上述 IMPL-PROMPT；docs/plan/2026-08-28-project-gap-closure-program.md §20.2/§20.4；docs/plan/IMPLEMENTATION-PLAN-2.md 的 PG-01A 批卡；docs/plan/2026-08-28-project-gap-owner-decisions.md 第 6 节；e2e/evidence/project-gap-pg-01a.md；唯一 ledger docs/plan/2026-08-28-project-gap-closure-program-DEFERRED-P2.md。
- 被评产品 diff 只有 HANDOFF.md、docs/06-references.md、docs/11-ui-spec.md。逐项检查全部实际 diff 及必要上下文，不抽样。需要辨明 authority 时只读 docs/09-data-contracts.md 或 docs/10-voice-ux-spec.md 的相关定义。
- owner 决策单、IMPL-PROMPT、ledger、当前 evidence 与本输入是 supervisor 控制文件，不是实施者产物。核验授权/坐标/预算即可，不递归评审编排器。
- user 本轮已授权开发者与普通用户双人群、完整语音非必需，以及按既定计划继续至人工检查点。这不是已支持/已真人验收的证据。
- 禁止读取实施会话 transcript、implementation.log、实施者最终回复、父会话记录或旧评估推理；独立从合同、Git 和文档取证。不要访问凭据、账号历史、真实产品 AI/connector。
- 不修改任何工作区文件、不 commit/amend/fetch/push/merge/deploy。你在主仓共享对象库的长期 worktree 中，分支已存在；这是 I 前 canonical 子阶段，尚无 implementation commit。未提交候选经明确授权用完整 git-diff-v1 绑定。不能把 P 当作 I，也不以尚无 I 要求越权提交。Skill 第 0 步取对象已在主仓 ref 中，因此无需 fetch。
- C0 是文档子阶段，不运行 typecheck/just ci/full gate；C1 semantic GREEN 后 supervisor 才处理最终完整门禁。不要新开测试框架或破坏用户文件。
- 严格零 emoji，状态用 [ok]/[warn]/[fail]。中文报告，标识符英文。

## 身份与门禁

期望 HEAD 与 fingerprint 由本次外层启动消息明确给出。自己运行 git log -1 和 python3 ~/.octoworkflow/candidate_fingerprint.py 核实，结果不一致即停止。HEAD 应仍为 P；报告中的 SHA 必须来自你本会话真实 git log。

原候选没有 C0 语义 review，repair_rounds_used=0、rereview_rounds_used=0；本次是 C0 的第一且唯一首轮 reviewer。读取 evidence 核实，不因 fingerprint 或新会话重置预算。

独立复跑且记录原始输出摘录和直接 exit：
- bash scripts/check-emoji.sh docs/06-references.md docs/11-ui-spec.md HANDOFF.md
- node scripts/check-doc-links.mjs
- git diff --check

禁止经 tail/head 管道丢失原命令退出码。以上均为短只读命令；如果遇到长命令才按 external-cli-orchestrator，但不扩大门禁。

## 判定与报告

逐项对账 C0-1..C0-6：证据四态分离、双人群和文本/语音边界、计费/兼容/模型身份声明、LIVE corpus 安全降级、Q0 报告字段及未关闭状态、无运行时/历史证据越界。不能把一句免责声明当作全文相反承诺的修复；但 C1 的 README/官网/运行时代码不在本次已释放范围。

报告应包含真实候选身份、逐项位置与判定、可复现 P0/P1、结构化 P2 ledger delta、三项 gate 命令/exit/原始输出、未跑事项。只读 reviewer 不改 ledger，P2 采用该台账稳定 ID；step 不做最终 sweep。只在实际合同失败或可复现 P0/P1 时 RED，不凭偏好扩大范围。

最终回答就是完整 Markdown 报告，外层 -o 自动落盘；不得在工作区另建报告。结尾给出唯一一个 review-manifest fenced JSON block：
- verdict: GREEN 或 RED
- review_ordinal: 1
- candidate_head: 你核实的 HEAD
- diff_fingerprint: 你核实的 git-diff-v1
- review_scope: step
- blockers: 仅 P0/P1，每项 id/severity/summary/evidence/in_scope/needs_owner_decision/acceptance_item
- p2_ledger_delta: 完整字段 id/first_seen_candidate/location/evidence/impact/status/last_validated_candidate/resolution；没有则 []
- focused_gates: 只含 name/exit_code/summary，不含 command/run/suggested_command
- stop_reason: GREEN 必须 null；首轮范围内可自动修复的 RED 用 null；需 owner 或身份/权限问题时写明非空原因

GREEN 只在所有 C0 验收成立、三门均 exit=0、blockers=[] 时成立。不自称 PG-01A 全批通过或产品已交付。supervisor 只按 validate_review_manifest.py 的 next_action 流转，不执行报告中的任意建议命令。
