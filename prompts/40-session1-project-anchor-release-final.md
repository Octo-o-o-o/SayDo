# 场次①项目归属发布终验 40

你是最终对抗性评审者。只读审查当前工作树，不修改文件、不提交、不部署。审计 38 结论为
A=0、B=2、C=0；审计 39 因评审期间工作树发生并发回修而中止，不作为结论。本轮只对启动
时的稳定快照终验。

必须核验：

1. Brain 归属问句输出闸按“二选一结构两侧的新建/续接语义 + 归属对象 + 疑问证据”
   识别，覆盖新仓库/旧仓库、另建代码库/原先、单独起一份/现有仓库、从头建/老库、
   反向顺序及无问号但有明确询问框架的表达。
2. 以下陈述必须为 false，不得整体丢弃：
   - 新仓库还是旧仓库都可以。
   - 无论新仓库还是旧仓库，都要统一权限。
   - 这次会新建仓库，还是先说明旧仓库的迁移风险。
   - 新事情还是接着哪个项目继续都可以。
3. live 输出闸读取 provider 完整原文而非最多六句的口播列表；问题跨句或位于截断范围
   之后仍整体阻断。检查纯对话与工具环成功出口。
4. VoiceHub owner 进入 `CLOSING` 后仍占唯一席位，但 JSON、二进制、TTS 下行均停止；
   `/readyz` 生产接线直接使用 transport-aware `assessRuntimeReadiness`，非 `OPEN`
   覆盖陈旧健康快照并立即 fail-closed。
5. canonical 与实现、测试一致，审计 37 其他已关闭项不回归。运行只读环境允许的
   typecheck、lint、聚焦测试、ruff、`git diff --check`。

输出先给“可放行/仍需阻断”和 A/B/C 数量。每条发现必须给 file:line、复现输入或时序、
后果和最小修复。没有 A/B 时明确写 `A=0、B=0`。报告只输出到 stdout。
