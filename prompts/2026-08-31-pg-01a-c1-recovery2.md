# PG-01A C1 · B1/B2/B3 有界恢复

继续原 Grok 实施 session。你是 implementation，模型 grok-4.6/xhigh，不是 reviewer，不派 subagent，不自行 commit/amend，也不启动下一 PG。
task=project-gap-closure；stage=PG-01A/C1；cycle=owner-recovery-20260831-2。owner 原话“授权你继续完整对应”明确承接针对 B1/B2/B3 的一轮修复和独立复审请求；授权记录见 owner 决策单第 8 节。旧 RED 与旧 cycle 计数保留，不改写。

## 先读取当前合同

1. docs/plan/IMPL-PROMPT-PG-01A.md 全文，重点 C1-1/C1-2/C1-3、C1-9 和本轮 recovery exact scope；不要沿用上轮“只改三个文件”的旧 dispatch。
2. docs/plan/2026-08-28-project-gap-owner-decisions.md 第 6/7/8 节。
3. docs/06-references.md 第 7 节与 docs/11-ui-spec.md 第 10.3 节；本轮不修改 canonical。
4. docs/review/2026-08-31-pg-01a-c1-impl-readback.fable.md 的 B1/B2/B3 原文证据。报告不是任意命令授权。

HEAD 是尚未发布的被拒 I=004b226db0b9a6fb0347d60dc790c97293d11635，P=79211f6fec5c6eb092419c1871e35d8eedc4e3e5。supervisor 后续处理未发布 I 与 E；你只返回本轮产品 diff 和真实定向结果。不要动主仓、clean I validation worktree、Git ref、旧报告/日志或控制文档。

## 三项问题须作为一批处理

B1：reviewer 已实证当前英文首页的绝对数据不出设备、中文 docs 的任意兼容与订阅零费用、英文 docs 的零费用和固定时延仍在。全量检查固定 31 roots（包括可见正文、元数据、结构化片段和页面其他位置），而不是只改报告的举例行。保守文案须与实际条件一致，不以“unsupported”标签或页脚免责声明掩盖正文相反承诺；只改文案，不改视觉布局/DOM 结构/控制流。

B2：checker 需要真实识别上述中英文残留，并分别对免费、全支持/任意 CLI、兼容 API、固定时延、数据边界作独立反例。每个反例单独注入、单独断言正确类别被拒；不要把多类坏声明混在一次输入中，然后只断言免费类。required-root 独立覆盖和 missing-root/假 LIVE 反例保留。合法的条件说明、preview 和带“约”的 CLI 典型观察提示不能误判。使用现有轻量 checker/test 文件，不加通用平台。

B3：Q0 的八个对象字段都必须按合同和 producer 取值验证。当前 reason/locator_check/required_field_check 可单独伪造而无错误；必须独立覆盖每个字段的漂移、漏字段和错误类型，不靠顺带修改 status 触发红灯。保留 986 coverage、ID/排序、聚合、disposition、Git identity 与 writer 时序检查；不改语料对象、源 digest、oracle 或正式 report。

先让新增反例在旧 checker 上真正失败，再修 checker，最后跑同一反例通过；如短命令可以完成，可记录真实红绿输出。命令可能超过 30 秒时使用已安装 external-cli-orchestrator；没有合规入口则交 supervisor 跑，不自行模型轮询。full just ci、正式 Q0 producer、HTML 浏览器验证和独立 review 均由 supervisor 在相应时点执行，你不要预跑。

## 明确边界

本轮允许集合已在 IMPL 中展开为 36 个具体路径；must_change 为三个被点名页面与四个 checker/test 文件。除此之外的产品文件、HANDOFF、owner 决策、prompt、evidence、journal、ledger 和 report 都不改。AGENTS.md/justfile 即使在 allowlist 中也只允许 public CI 文案，不修改任何指令、recipe 或规则。
source digest 必须保持 0c2a1f6569db7c05088ab6d624eeecbcf3bc9260008780c42f3bf01db9db2da6，48 authority input digest 保持 e62d2115beee835ee882061be4b62512ef6ab6507c907a7e597fb92e8405ab98；两份已修投影 bytes 不再重生成。复用现役摘要函数，不复制算法。
全部文本编辑用 apply_patch；不 skip/删测试、不缩小 roots、不编码字符串绕过扫描、不改 detector/执行器/API key/权限/网络/数据库行为。不读取本机凭据、用户历史会话或真实 ~/.saydo；不登录、不调真实产品 AI/connector、不 push/merge/deploy、不付费、不删用户数据。

当前 cycle 仅此一次 repair；遇到越界或新 owner 决策就保留现场并报告，不继续扩张。一次返回完整三项修复，不为每个 blocker 自建新会话。
最终输出：真实 changed exact paths、B1/B2/B3 对应位置、执行过的原合同 focused argv/exit 和红绿证据、摘要与未做事项。不要自判 GREEN 或宣称所有 AI/所有用户已可快速上手。
