# 场次①项目归属发布终验 46

只读审查当前稳定工作树，不修改文件、不提交、不部署。审计 45 结论为 A=0、B=1、C=0；
唯一 B 项是实现错误接受“前句以 connector 结尾”的跨句窗口。该分支现已删除，并加入
`新建项目还是？沿用旧项目。` 必须为 false 的回归测试。本轮对回修后的稳定快照终验。

核验：

1. 项目归属问句必须是恰好两个完整 alternatives，一侧 full-match 新建分支，另一侧
   full-match 续接分支；跨句只允许“前句一侧 + 后句以 connector 开头”，不得接受前句
   connector 结尾。
2. 正例包括既有新仓库/旧仓库、反向顺序、相邻跨句、无问号明确询问框架、成对要么、
   “沿用原先的/接着之前的/继续这个项目”；普通陈述、无关问号、嵌套成本/决定问句、
   `新建项目还是？沿用旧项目。` 均不命中。
3. live 使用 provider 完整原文，在截断/分句前阻断；纯对话与工具环成功出口一致。
4. CLOSING owner 占席但 JSON/binary/TTS 全阻断；`/readyz` 使用 transport-aware helper。
5. canonical、实现、测试一致；审计 37 其他关闭项不回归。运行只读环境允许的门禁，
   临时目录不可写须作为环境限制记录。

先给可放行/仍需阻断和 A/B/C。发现须给 file:line、复现、后果、最小修复；无 A/B 时明确
A=0、B=0。报告仅输出到 stdout。
