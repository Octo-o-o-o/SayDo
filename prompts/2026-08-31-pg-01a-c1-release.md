# PG-01A/C1 明确释放

恢复同一实施 session=97B1C3A9-AA21-42DC-AF22-3DB75985FEA9，不建立新实施线，不用 --restore-code。
C0 ordinal 2 独立一致性 review 已 GREEN，validator 实测 exit=0/status=valid/next_action=full_gate；B1 closed，P2 delta=[]，C0 三门均为 0。C0 文档子阶段没有 just ci 门；其产品级完整门禁按原合同留到 C1 语义 GREEN 后。

现在 supervisor 明确释放 docs/plan/IMPL-PROMPT-PG-01A.md 的 C1。完整重读当前合同的 C1-1..C1-7、exact path set、focused argv 与禁止范围，保留 C0 已收敛的 canonical。
task=project-gap-closure；stage=PG-01A/C1；cycle=owner-continuation-20260831。
整批 repair=1、rereview=1 已用完，不因进入 C1 重置。你可以在本次初始 C1 实施中正常迭代自己新增的 focused 检查；交出候选后若再有 RED，由 supervisor 停 owner，不能自动第三轮。

本阶段只做公开 claim 止损、active-claim checker/mutation、Q0 truth report producer/checker/mutation、corpus 安全降级及必要投影。开发者与普通用户是首批目标、语音可选，不写成普通用户或所有 AI 服务已开箱即用。不实现 PG-01B/PG-02/PG-06 的 runtime 功能，不改变 API key、模型 admission、权限或默认 UI 控制流。
AGENTS.md 与 justfile 只修改本批指定的“本地 CI 等效”失实说法，不借此搭建 CI/control-graph 框架。console 只改 copy 和允许范围内直接对应断言，不改布局/控制流。

先前 supervisor 直接调用现役 hashCorpusSourceTree 得到 C1 前 source digest：
0c2a1f6569db7c05088ab6d624eeecbcf3bc9260008780c42f3bf01db9db2da6。
自己复核；源 digest 改变则按合同调用现役 rebuild-three-pass-dry-run.mjs，不变不能改两份投影。不得复制摘要算法或改 oracle。

独立 worktree 已用 pnpm install --frozen-lockfile --offline 准备依赖，exit=0，Node v22.23.1、pnpm 10.33.1；不改 lockfile/版本。
C1 合同补入了已核实的 console typecheck、eslint、vitest argv；有 console TS/TSX 改动则这些受影响 package 门必须跑。测试编辑仍受 exact set 约束，越界先报告，不自行增路径。
短只读门可直接执行；超过 30 秒的测试/构建必须按 external-cli-orchestrator 规范，若 Grok 宿主没有单次 PTC 等待能力，停止等待并把 exact argv 交 supervisor 跑，不模型定频轮询。只跑 focused，禁止 just ci/full gate 预跑。

当前无本地 commit/amend 权限，因此 implementation SHA/tree 仍为 n/a；不得取 P 冒充 I，不创建自指 report。
Q0 新 checker 与 mutations 应在临时合成 fixture 验证缺报告/重复漏项/总数与字段漂移/误标 valid/身份不符会红；真实的 --write E report 等 I clean/门禁后由 supervisor 运行，当前不能生成带假 I 的产物。临时测试仓可按合同创建 fixture，不可影响主仓 history 或真实用户目录。

不读取账户 credentials、history 或真实 ~/.saydo，不请求模型/connector，不登录、不部署、不开新远程入口、不付费、不删用户数据。不改 control 文件、旧报告/日志/evidence。所有本地文本编辑用 apply_patch；生成器仅按合同重建其自己的产物。

HANDOFF 保持 active=PG-01A,next=none，准确记当前 C1 与未提交 I 状态。最终交出实际 exact changed paths、验收逐项证据、focused 命令/真实 exit、source digest 前后值、未跑/阻塞事项，然后停止。不自评 GREEN，不自行发 reviewer/提交/启动下一 PG。
