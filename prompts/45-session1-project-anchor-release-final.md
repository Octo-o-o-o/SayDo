# 场次①项目归属发布终验 45

只读审查当前稳定工作树，不修改文件、不提交、不部署。前序终验 39–44 因主会话按独立评审
持续回修而中止，不作为结论；两份最终独立复审现均为 A=0、B=0、C=0。

核验：

1. 归属问句必须恰好两个 full-match alternatives，一侧新建、一侧续接；句内或 connector
   开头的相邻跨句窗口绑定疑问证据。
2. 成对 `要么 A，要么 B` 仅定向规范化首个 `要么`；带问号命中，无标点陈述不命中。
3. 句首 `请问` 仅在两侧 full-match 后算独立询问证据；“继续这个项目/接着这个仓库”、
   “沿用原先的/接着之前的”命中；嵌套成本/决定问句和无关问号不命中。
4. live 使用 provider 完整原文并在截断/分句前阻断；纯对话与工具环成功出口一致。
5. CLOSING owner 占席但 JSON/binary/TTS 全阻断；`/readyz` 使用 transport-aware helper。
6. canonical、实现、测试一致；审计 37 其他关闭项不回归。运行允许的门禁。

先给可放行/仍需阻断和 A/B/C。发现须给 file:line、复现、后果、最小修复；无 A/B 时明确
A=0、B=0。仅 stdout。
