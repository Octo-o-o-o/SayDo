# SayDo Focus 交互结构定稿 v1

> 2026-08-04 深夜,义骁x既白五轮 Demo 对齐收敛(v1->v5)的书面定稿。
> 演进痕迹:[v1](2026-08-04-Focus主页Demo-v1.html)(三页骨架)->[v2](2026-08-04-Focus主页Demo-v2.html)(大卡/航迹图/会话易耗品)->[v3](2026-08-04-Focus主页Demo-v3.html)(人话化/线操作/依赖)->[v4](2026-08-04-Focus主页Demo-v4.html)(空间/收官/归档)->[v5](2026-08-04-Focus主页Demo-v5.html)(定稿:空间下拉/新建双通道/任务详情弹窗)。
> 本文是**交互结构合同**,供方案 v0.3(数据与实施)引用;冲突以本文+v5 Demo 为准。

## 1. 信息架构(五层封顶)

```
空间(Workspace) > Focus > 线(lane) > 卡(账本事件/义务) > 产物(文件,git 管)
```

- **空间**:Focus 的分组,**单归属**(每个 Focus 归一个空间);上下文切换语义(我现在坐到哪摊事跟前),不是过滤标签。「全部」视图保底。空间可新建/重命名/删除(删除=其内 Focus 移回「全部」,不删 Focus)。UI=下拉(可能很多,不横铺)。
- **Focus**:意图与承诺边界,对话的主轴(会话主轴倒置:session 锚 Focus,不再锚 project)。Focus 之间**不交叉**——跨 Focus 依赖降级为"外部等待"事项,不画结构线。
- **线**:Focus 内并行工作流。**非用户概念**:缺省一条,AI 拆解提议(经确认)才浮现多条;用户价值=透明度(一眼看出 AI 在怎么推进、哪条要自己出手)。结构上是**树**(从某线分出),运行依赖是**等待**(gate:排队卡引用前置,"那边一落定这边自动开跑"),不画任意有向图。
- **卡**:账本事件=一次写入(记事/办结/接上/派活/收尾/方向更新)。历史不可改写;调整(整线移除/整线重来/从某步重走)=新记录冲销旧记录,也长卡、也可再冲销(跨会话无 durable undo,一审 A10;会话内 undo 栈维持现状)。
- **产物**:文件,活在关联项目的 git 仓;卡挂 commit hash/路径作指针。**账本管"发生了什么",git 管"产出了什么"**——借 git 概念(不可变/CAS/分叉/tag),不借 git 实现(结构化语义对象需要查询层与字段级 CAS,SQLite event log 已是领域特化的"语义 git")。
- **project(关联项目)**:资源与安全边界(workspace 路径/执行沙箱/只读工具的墙),降级为 Focus 的属性,多对多(focus_project_refs),与空间正交。

## 2. 页面与导航

| 页面 | 定位 | 关键元素 |
|---|---|---|
| **看板主页**(入口) | 进展 Dashboard,非对话 | 空间下拉+「+ 新 Focus」;「今天需要你」跨 Focus 聚合(只列要人出手的);分组卡流:进行中(大卡)/等外部(中卡)/已收官(产物卡)/归档(极轻行) |
| **Focus 详情** | 单 Focus 的透明度 | 头卡(方向/方向 vN/账上 N 件/关联仓库/预期产物)+「开新对话续推」;对话小节(易耗品清单:已收尾xN+进行中);**进展航迹**(线x事件序,非均匀时间,静默折叠,行末=当前球权,线菜单=三种调整,gate 排队卡) |
| **对话+记录流** | 输入通道 | 左对话(语音/打字/确认卡);右「这次聊出来的东西」(办成事才长卡,纯聊不长);从详情进=新会话从账上续起 |
| **任务详情**(弹窗) | "我该从哪进去做" | 从看板条目/航迹当前态卡直达;三型:**拍板型**(选项卡+任务内对话+办结闭环)/**在办型**(进度+叫停+追加指示)/**启动型**(怎么开始+标记完成);完成即闭环(条目消失/航迹长一步/AI 接续) |
| **新建 Focus**(弹窗) | 两种开法 | 「填好再建」(名字/方向可空/空间/关联项目可选)/「从空白开始」(直接开聊,说着说着成形,AI 提议立 Focus 经确认) |

## 3. 球权四色(统一语言,贯穿全部页面)

| 色 | 语义 | 数据来源 |
|---|---|---|
| 绿 | AI 在办(你等着,到点叫你) | agent 义务 in_progress/任务 running |
| 橙 | 等你拍板/配合(AI 被你卡住) | pending 确认/等验收/AI 开给你的信息请求 |
| 蓝 | 等你启动(还没推到 AI 能接的步骤) | human 义务 open 无 AI 依赖 |
| 灰 | 等外部(第三方,有信叫你) | external 义务 |

三色计数**只出现在**「今天需要你」与 Focus 详情;Focus 列表卡只留一句话球权("下一步需要你:X"/"不需要你")。

## 4. 生命周期

captured(刚建)-> active(进行中)<-> **archived(归档,新增态)** / dormant(等外部)-> closed(已收官)|abandoned(放弃)

- **收官**:善终,账已结;**不可重开,可续开**(「以此为底续开新一轮」=fork 新 Focus,带产物与经验);展示以产物为主,航迹退幕后。
- **归档**:中途主动放下(账未结,系统不盯);**可重新打开**回 active;极轻展示+归档理由。需从 abandoned 拆出 archived 态。

## 5. 术语人话表(UI 文案规范)

| 系统概念 | UI 人话 |
|---|---|
| obligation / resolve | 记下的事(记下一件事)/ 办结(完成·放弃·不再需要) |
| open obligations xN / 挂账 | 账上还有 N 件事 / 留到下次继续 |
| revision N | 方向 vN(方向更新) |
| anchor / close settlement | 接上 / 收尾 |
| swimlane / entity feed | 线 / 这次聊出来的东西 |
| active/dormant/captured/closed/archived | 进行中/睡眠·等外部/刚建/已收官/归档 |

原则:名词全人话,「账」是唯一保留的隐喻锚;Focus 保留英文(既有拍板)。

## 6. 核心交互原则

1. **办成事才长卡**:写入(记事/办结/接上/派活/收尾)->右侧记录流长持久卡;纯问答不长——记录流不掺水,每张卡都值得信。
2. **对话是输入,实体是结果**:外层非对话(看板),内层对话(输入通道);确认不念操作说明(语音只念提议核心,操作在确认卡上点)。
3. **会话易耗品**:每场从账上续起、收尾即弃;上下文永不"太长",账本才是持久物。Fork 两义:并行开工(同 Focus 多会话,CAS 兜并发,A5 修复后安全)/Focus 分裂(拆新 Focus 义务分家)——**分裂经 codex 一审 A12 移入待设计**(v0.3.1 不承诺),并行开工入 v0.3.1。
4. **历史不可改写**:一切调整=追加冲销、可再冲销;时间轴密度不均匀(事件序,静默折叠),未来只画一小截,不做假排期。

## 7. 待设计清单(定稿明确不含,防散焦)

通知/回叫推送形态(今天需要你的增量如何叫人)· 全局搜索 · 在办型/启动型任务弹窗细化 · 收官 fork 的预填交互 · 移动端形态 · 空间删除的 Focus 迁移交互 · 成本视图并入位置。

## 8. 与 OctoDesk Work Steward 对比与互鉴(2026-08-04 深夜,义骁指路)

> WS=OctoDesk 工作总管执行面 v2(机制 SoT:OctoDesk/docs/plan/2026-07-10-work-steward-final.fable.md;五 flag 默认 off,owner 库已全开)。**先做好 Focus,WS 不急**——但两边已各自趟出的坑值得互认。

**关系判断:同一世界观的上下两层,不是竞品。** WS=执行层治理(怎么**安全地替人做事**:计划->步骤->工具矩阵->物化审批->收件箱);Focus=意图层记账(怎么**跨时间对齐人机承诺**:Focus->线->义务->收尾)。WS 的单位是一次性的**计划**,计划之间没有账;Focus 的单位是跨会话的**意图**,恰好补上这层。理想终局:Focus 是总账,WS 是"办公自动化类义务"的执行适配器。两个项目从两端向同一点会师——WS 从结构化 GUI 走向自然交互,SayDo 从对话走向结构化看板(本定稿即是)。最强的独立收敛证据:WS 的**注意力收件箱**(待批/停靠/等待输入/人工待办/预算/brief 六类)与 Focus 的**「今天需要你」**(拍板/启动/配合三色)是同一发明的两次独立发生。

**Focus 应向 WS 借的(方案 v0.3 直接引用)**:
1. **所见即所签**:批准必须携带展示版本 digest,失配自动转回重新准备——Focus 确认卡应加同款(防"确认内容与执行内容漂移");
2. **物化审批状态机**:参数绑定+单次消费+执行前重校(ApprovalTokenStore 同型纪律,Focus receipt 已有单次消费,补执行前重校);
3. **触发器频控参数**:5min 去抖/每源每小时 6 次/日上限/30 天 sourceRef 去重——Focus 事件订阅唤醒直接抄;
4. **模板白名单+能力上限**(触发计划恒 allowExternalSend=false):AI 自发开线/记事的白名单思想;
5. **kill-switch 服务层强制**(读前短路+事务内复查闭 TOCTOU):Focus 撤销/回滚语义同款纪律;
6. **注意力项补型**:「预算」(成本超水位叫人)与「brief 摘要」两类是「今天需要你」该补的。

**WS 将来可向 Focus 借的(登记不动工)**:跨计划连续性层(晨报叫你做的事做了没——义务账本)/会话易耗品哲学/球权四色的用户视角分型(六类系统视角->按"球在谁"重组)/航迹图(步骤清单->线x事件x球权)/人话术语表平移。

## 9. 开发推进(按协作规范)

本定稿+v5 Demo -> 方案 v0.3(会话主轴倒置/主页信息架构/archived 态与 fork/确认卡所见即所签/空间模型)-> codex 对抗 review -> 实施计划 -> codex 对抗 review -> 执行 prompt -> 交执行 AI。dogfood 期 dev 实例照常运行,cutover 级变更(session 主键倒置)不在 dev 上热改。
