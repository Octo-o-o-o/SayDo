# SayDo 首发 · 会话交接文档(HANDOFF)

> 重写 2026-07-25(收口会话):旧版 §3"后续待办"、§4.1"ASR 阻塞"、§6"切锁待做"、§5"Codex 攒批待做"
> 均为中途旧段,与 §1 自相矛盾——已按 git log + 运行时配置 + 独立对账裁决清理(裁决记录:
> `e2e/evidence/closeout-verification.md` §3.1)。本版为**单一真相**:所有状态均经收口会话独立核实。
>
> **开工先读**:本文件 → 本仓 `AGENTS.md` → `docs/plan/IMPLEMENTATION-PLAN-2.md`(**唯一排产源**,W1–W9 + 合同轮)→ `docs/plan/IMPLEMENTATION-PLAN.md`(首发计划,已收口,仅作出处索引)→ `docs/09`(数据契约,照抄源)/`docs/10`(话术)/`docs/11`(UI)/`docs/modules/a-e`(分域导航)。
> 唯一活动仓 = `/Users/wangyixiao/WorkSpace/SayDo`(远端两个,2026-08-20 起:origin = 私有归档 `github.com/Octo-o-o-o/SayDo-archive`(全史,日常 push);public = 公开快照仓 `github.com/Octo-o-o-o/SayDo`,经 `scripts/publish-public-snapshot.sh` 更新,推前跑隐私探针);设计、实现、证据与过程档案均在本仓。旧路径 `voice-coding` 是指向 SayDo 的兼容链接，冻结冷档是 `voice-coding.archive-20260729`，映射见 `docs/plan/MIGRATION.md`。

---

## 0. 两条硬教训(任何后续会话必须贯彻)

1. **绝不在源码/测试写完整凭据形态字面量**(完整 AWS key / PEM 私钥块 / `sk-`+长串 / 手机号卡号 / `PRIVATE KEY` 连续词)——会触发平台实时网络内容防护,导致整个对话请求被阻断。敏感样本一律**运行时拼接构造**(见 `packages/daemon/src/voice/redactor.ts` 与其测试的写法)。
2. **每次 Write/Shell/提交后,用独立 Shell 命令核实真实落盘/真实 SHA**,不要基于"我以为写了"继续。首发实施会话曾多次臆想工具结果(HANDOFF、2.2 的 compiler/retrieval、多个提交 SHA 都曾是幻觉);收口对账另抓到 Gate 0 两行证据虚报(closeout-verification §3.2)——自报不可信,证据必须可复跑。

---

## 1. 状态:首发主体实现有证据；发布前回修已 commit/deploy，真人验收未收口

- **当前批次指针:**(空)。`w54a-claude-cli` 已收口(2026-08-20;评审 2 返工末码 `54b981cc0e38dbb4d66f8b5868761c208b3b1617`,evidence `e2e/evidence/w54a-claude-cli.md`;spike `e2e/spikes/claude-cli-tier1/`,fixture `packages/daemon/test/fixtures/claude-cli/2.1.220/`)。`cmdeffect-hardening` 已收口(2026-08-19;代码 `e79d1d8101be11e3bd6051841e6a37574cd38d37`,evidence `e2e/evidence/cmdeffect-hardening.md`;评审 1 返工 `068e392153d4bd571ce373dceb6adfa427290059`;评审 2 返工 `115353e48970a8ee818d4c1cfd189dd00f11a1c9`;O-1 落地 `adc2b9a88364df8f43198f856fa3cdd35f06adb2`)。`remote-mobile-w0` 已收口(2026-08-16;代码 `addfd1965a5144223df3bfa3f7c407976664929a`,evidence `e2e/evidence/remote-mobile-w0.md`)。完成定义=LAN `remote-mobile` 代码 + 临时 Chromium/LAN 证据,不部署常驻、不宣称真机狗粮。下一工程批 = **W5.4-b 生产执行主流程接线**(Claude 订阅不再阻塞)。A3-armed 已收口 2026-07-28,evidence `e2e/evidence/a3-armed-batch.md`(源码层结论,不等于本机 deep readiness 已武装)。
- **2026-08-21 当前快照(三层时钟；当前坐标唯一以本行为准):**活动树 HEAD `088b8f0cc176bc2be9cf09235a640301dd35cb55`；常驻 runtime `ada7981c67ef3a07e6df0431643bb8b7661e22d4`；release config digest `2278f7b9358d7dbfc20daaefceedb50b1b92f63a0f97ac2c40bc5a4b424dccac`。2026-08-21 00:20 `[t2]` / `listen` 变更已使旧场次锁再次作废,owner 尚未重新声明四场基线。本批不以提交收口,当前批次指针保持为空。
- **2026-08-16 全量验收(源码 live,非发布锁):**`e2e/evidence/2026-08-16-full-acceptance/`。`just ci` 绿;live 走查终端全 PASS;Playwright 夹具 9 绿 / 25 红仍是已知债。模型与 cap-token 在本机 `~/.saydo`,**不入仓**;换机须重新武装 Cursor CLI grok 4.6 / composer 2.5。真麦/S3/真机/writing 见该目录 `manual-retest-needed.md`。
- **P0 全部 + P0.5-A/B/C/E + 全量 readback 完成**:Phase -1/0/1/2/3/4/5、外部评审 M1-M11、P0.5 契约封闭/Hopper 桥/直达验收档/Demo 生成器。终账 `e2e/evidence/final-readback.md`;逐阶段证据 `e2e/evidence/phase-0..5.md` + `gate0-checklist.md` + `p0-readback.md` + `p05.md` + `review-m-batch.md`。
- **收口对账(独立复核,不信自报)**:`e2e/evidence/closeout-verification.md`——五路门禁全量复跑全绿(`just ci` 双矩阵 / Playwright 8/8 / fake-runner e2e 3/3 对锁定二进制 / golden 43 条 / 音频烟测 5/5);final-readback 逐节抽验成立;发现并修复 Gate 0 两处证据问题(audit_log 触发器 `0eb96f1` / privacy 接线)。
- **运行基线**(收口会话实测):contracts 65 + daemon 344(+1 skipped:SAYDO_SLOW_E2E 门控慢测试)绿;Hopper 锁定副本 `~/.saydo/hopper-dist` @ `bdd1e548`(baseline.2);`~/.saydo/config.toml` expected_version 已切锁 `bdd1e548…`。**增量基线**:接线批后 daemon 393;执行器批后 contracts 66 + daemon 452 | 2 skipped(2026-07-26 对账会话亲测),evidence `e2e/evidence/executor-batch.md`;**W1 批后 contracts 66 + daemon 467 | 4 skipped**(+15;skipped +2 = 新增门控 live e2e:0.0(a) hooks/resume),evidence `e2e/evidence/w1-batch.md`。**单仓迁移后基线(2026-07-29)**:contracts 73 + daemon 687 | 4 skipped + Python 20；**发布前代码提交 `b20151440011ce0452417439c2d81745cb5d7d39`** 的独立 clean worktree `just ci` exit 0，contracts 73 + daemon 700 | 4 skipped + Python 25。
- **ASR 已定档**(2026-07-24 晚 owner 提供三元组后结项):火山 sauc 流式,热词偏置 +10 点术语召回(65.3%);工程 ADR-101 定稿;`e2e/spikes/asr-1.0/RESULT.md` 两轮跑分明细。第二家对比 = 换 provider 门禁,非首发(Codex 12 A3)。
- **canonical 回写**:M 批文档侧经 Codex 13/13b 复核(3A+7B 全回修);其余五处 09 回写 + 切锁/reviewTask 两条新勘误经 Codex 14 攒批(见 `research/codex-findings/14-canonical-writeback-closeout.md`)。
- **W1 批(收尾与 dogfood 起步)已收口(2026-07-26)**:九项全落(audio-smoke 收编/误听回归集/项目层配置生产加载/挂账清偿含 0.0(a) 补验/周报增量含 W9 触发线栏/TTS 尾项/HANDOFF 回填/转写 ref 口径统一/排产源移交)+ 批末 code-review 1A2B 全回修;零 canonical 变更(红线守住);evidence `e2e/evidence/w1-batch.md`。
- **W4 批(S3 卡 + writing 窄版 + 增量 3.7/3.8/3.9)已收口(2026-07-27,`eabc5ac→07377a4` 8 提交;evidence `e2e/evidence/w4-batch.md`;批末 code-review A 级两条(v11/v9 迁移对老库健壮性)已全修 `07377a4`)**:S3 屏幕审批卡全链(WebAuthn:v9 两表+approvals 六列双向 CHECK+turn_ref 放宽 / 四工具 / assertS3LocalAndBound 四断言 / console Touch ID 卡 / §12-13 反例 32)· writing 窄版(project+writing/artifact article/WritingSettleProof+barrier 五断言/executor 按 type 分叉/reviewTask writing 分支/类型门禁/v10 迁移/content_done/逐条裁决 UI/§12-12+14 反例 14)· golden 扩 W4_GOLDEN 4 · readinessSkeleton 单源+生产门(§12-15 反例 12;证据绑定 canonical 留白)· proposed TTL(v11 迁移+双闸+调度器扫;§12-1 A6 反例 8)· 跨项目并发守恒 · 输入区三态(turn.text 通路)· writing 全链闭合 e2e。**基线:contracts 73 + daemon 638|4 skipped + python 20 + playwright 21**;迁移 v9/v10/v11 additive+表重建(老库收敛/剔除加固)。**2026-07-29 现场状态**:`enabled_project_types=["coding","writing"]` 已生效；仍待 S3 卡真人过卡与 OctoBlog 首篇逐节验收，生产库尚无 writing 项目或 task。canonical 待回写 8 条已随 R-A 收口落盘。下一批当时指向 PLAN-2(W5 剩余;现已收口为 W5.4-a,当前下一项 = W5.4-b 生产执行主流程接线、5.6/5.8/5.9 挂 §6 二次确认)。
- **W5a 前段批(W5 合同就绪子集 + TTS 拍板)已收口(2026-07-27,`086001d→0a64170` 11 提交;evidence `e2e/evidence/w5a-batch.md`;批末 code-review A 级零 + 1B 竞态守卫已修 `0a64170`;当时登记的 canonical 8 条已随 R-A 落盘)**:3.0 TTS 音色切 tianmeiyueyue(HANDOFF §2-10 清账)· 3.1 verify 两条安全债清偿(config 冻结闭包 + env 隔离,conformance §3/§4 改 [ok])· 3.2 decisions[] 第三层 + openOnScreen 编辑器深链(口播/上屏同源)· 3.3 edit 审批第四动作(§12-3 正反例 + console 交互)· 3.4 steer cancel_resume + Hopper capabilities 分级消费· 3.5 项目级模型/预算覆盖(daemon 受控表 v7,绝不落 project.toml)+ cache_write 列位· 3.6 产物控制面(时间线/diff/子集导出 + Playwright 三用例)· 3.7 微项篮(订阅限流 durable 排队 v8 + 紧凑模式;C8/E3/gist/A7/评估 spike/D9/golden 批容量顺延登记)。**基线:contracts 67 + daemon 564|4 skipped + python 20 + playwright 14**;迁移 v6(decisions_json)/v7(project_settings)/v8(subscription_retry_queue)additive。当前下一批以 PLAN-2 §7 为准。
- **W2 批(提前批四项 + 安全五件 + 场次①修复)已收口(2026-07-26)**:阶段 0 安全五件(Codex 20 移交,场次②前置已清)+ A launchd(装载 + kill 自启实测;**运行时/开发树分离**:常驻走 `~/.saydo/runtime` 独立树,`just daemon deploy` 更新——dogfood 时段不重启,§4 惯例)+ B T2 薄版代码收口(tailnet 白名单/S3 手机面拒/ntfy 投递与深链;**组网与手机烟测 owner 暂缓**,待办见 §2-12)+ C M1 完整版(OctoDesk 真仓奠基 gen 3 + 生长闭环 + m1-notes 投影)+ D VAD 免手(能量 VAD + 语义 EOU + 说完了按钮;真麦留场次)+ **场次①现场发现修复批**(DDL v5 迁移铁律/Chat 排序/思考中指示/B1B2 缓解/悬挂包处置/Dashboard 新对话/C3 延迟核对——owner 拍板等 Gemini 解封);E(S3 卡/writing 窄版)按合同门纪律顺延 W4。批末合并终审 1A+1B 全回修 + canonical 回写七处经一致性评审 0A5B 全回修(Codex 攒批登记下一轮补发)+ **迟到评审回收批 `fd3918d`**(三路后台评审迟到返回,净新增 3A 全修:tailnet 语音 S3 门/EOU 悬挂兜底/会话重绑;6B+C 组同批回收)。**基线:contracts 66 + daemon 520|4 skipped + python 20**;evidence `e2e/evidence/pull-forward-batch.md`。本句只记历史批次出口，当前下一批以 PLAN-2 §7 为准。
- **2026-07-29 常驻现场快照**:daemon/pipeline 均 running，`/health` 返回 ok；runtime clean @ `838aeea`，活动仓 @ `f28489d`，迁移提交中的知识底座兼容修复尚未部署。生产库只有 coding/active 1 + pending/draft 3，session 2、decision package 3、artifact 3，task/approval/readiness assessment/active readiness binding 均为 0；这证明服务可运行，不证明场次①–④已通过。完整快照见 `history/2026-07-29-migration-and-implementation-status.md`。
- **2026-07-30 发布前部署**:owner 已确认 A3 语义为 `gap_critical` 阻断、`gap_knowledge/gap_requirement` 仅提示，并分别授权 commit 与当前部署时窗。常驻 runtime 已切到 clean `b20151440011ce0452417439c2d81745cb5d7d39`；daemon/pipeline loaded SHA、`readyz`、ASR/TTS 启动探活、SQLite `quick_check` 与不派发 voice handshake 均通过。GitHub fetch 因 TLS 错误两次失败，祖先关系只能基于本地 `origin/main=f28489d…` 核对；push 未授权、未执行。部署记录见 `e2e/owner-sessions/runtime-deploy.md`。
- **2026-07-31 voice/runtime 真相复核**:常驻 runtime 仍 clean @ `b20151440011ce0452417439c2d81745cb5d7d39`，`/health` 与 `/readyz` 均为 ok，daemon/pipeline loaded SHA 一致，ASR/TTS 探活通过；但 live `~/.saydo/config.toml` 明确覆盖 `voice="zh_female_jitangmei_uranus_bigtts"`，因此当前实例不能宣称已使用源码缺省的 tianmeiyueyue。该 live TTS 配置由 owner 控制，本次 E9 只纠正文档、未改配置。任务路由合同与代码一致：`route=tier1` 必带 adapter，`route=hopper` 的 adapter 恒空且合并仍走人工交接。

## 2. 剩余事项 = 发布前部署门 + owner 项 + 后续外部解锁

| # | 项 | 说明 |
|---|---|---|
| 1 | **场次①②③④真人验收(owner 2026-07-25 拍板:严格四场分开,①→②→③→④)** | 现场清单 `e2e/owner-sessions/session-1..4.md`;#9 接线批 + **Tier1 生产执行器批均已完成**(2026-07-25)，功能主体已具备。认领 queued 任务起 cursor-agent 进程 + S2 审批门 live 上浮 + settle 回叫链 + 验收三态 + 人工合并全链已有自动化与历史真 agent 证据(`e2e/poc/tier1-live-executor/`;conformance `e2e/evidence/tier1-conformance.md`)；约场前另确认 `[tier1]` 两键。④仍是首发交付最终发布裁决点，通过后才可由 owner 授权打 `v0.1.0`。**2026-08-21 当前:**场次①已在 runtime `ada7981c67ef3a07e6df0431643bb8b7661e22d4` 上运行且结果为 `failed`，返工已部署，待 owner 从步骤 1 复验；场次②–④为 `not_run`。2026-07-31 授权部署已把当时的发布锁从 `b201514…` / digest `522e0716…` 前移；2026-08-21 00:20 `[t2]` / `listen` 变更已使旧场次锁再次作废，owner 尚未重新声明四场基线。 |
| 2 | ~~真人音频底板 5 条~~ **已到位并烟测(owner 录 5 条 m4a;2026-07-26 凌晨由计划会话代跑,W1.7 回填)** | 结果 **3/5(两次复跑稳定,非抖动)**:a01/a02/a04 ok;a13 "settle barrier"→"strawberry"、a20 "Gate 0"→"get 0" 稳定误听——已入 golden 回归集(`e2e/golden/asr-regression.mjs` r001/r002)+ 热词调优五组变体实验无改善(W1.2,evidence `w1-batch.md`),拉回走用户纠错链(10 #8/#9),场次①步骤 5 现场验;**不重录凑线**(底板要真实条件)。复跑:`pipeline/.venv/bin/python e2e/smoke/audio-smoke-5.py --profile owner`(3/5 known-miss 口径,W1.1 收编) |
| 3 | ~~dogfood 首个真实项目指定~~ **已指定(owner 2026-07-26 凌晨,双项目双类型;W1.7 回填)** | `~/WorkSpace/OctoDesk` = coding 类型(场次②起日用;W2-C 的 M1 奠基对象,执行前知会 owner)、`~/WorkSpace/OctoBlog` = writing 类型(随 writing 窄版就绪后接入,此前不奠基不派单)。**命名澄清**:`~/WorkSpace/saydo-dogfood`(commit `f8e36af`)是执行器批的 **e2e 独立测试沙箱**,不是 dogfood 真仓(纪律:e2e 绝不碰 dogfood 真仓;建议后续改名 `saydo-e2e-sandbox` 消歧义) |
| 4 | ~~工程 ADR-002 复核批复~~ **已批复(owner 2026-07-25:收窄改写；T18 后状态已更新)** | 条款收窄已回写 09 §11 规则 2:封闭枚举 + 豁免生效前提=可执行文件身份核验(预登记绝对路径+digest)+ 生效必落 `observed_model_exempted` 审计标记。BYOA 的 `verified_binary_default` 身份核验链与条件豁免已随 T18 落地,仅 `{codex_cli,claude_cli}` 且流内无 model 时可按合同触发；Tier1 `claude_code` 不使用该豁免。工程 ADR-002 的历史「休眠」状态陈述待随 W5.4-b 前置 canonical 回写校正。 |
| 5 | **GitHub Actions**(billing 已恢复;W1.7 回填的「8 月再开」已过期) | billing 已恢复。2026-08-20 run `32334615753` 能启动且 Python job 通过，Node job 因 workflow `pnpm/action-setup` `version: 10` 与根 `packageManager: pnpm@10.33.1` 重复指定失败。本地 `just ci` 仍是实质门禁。只记录，本批不改 workflow(修复须走公开快照仓通道)。 |
| 6 | **Claude**(CLI 已登录 Max;W5.4-a 已收口) | CLI 2.1.225，已用 Claude.ai 登录，Max 订阅，零 API key。**硬约束:订阅只经 `claude` CLI 登录态消费**(BYOA `claude_cli` 供给 + Tier1 `claude_code` 后端以 `claude -p` 子进程消费同一 CLI 登录态,不得配 ANTHROPIC_API_KEY 消费订阅、不与其他项目共用席位)。W5.4-a 只收口纯函数层，未接生产主流程；剩余是 **W5.4-b 生产执行主流程接线**。BYOA `claude_cli` 的条件豁免已随 T18 落地；Tier1 `claude_code` 不使用该豁免,恒 `observedModelExempted=false`。 |
| 7 | (可选)OpenAI key / OpenRouter 放行 | 解锁 ASR 第二家对比与 dialog 档缺省 gemini(现 dev 用 deepseek 异族合法) |
| 8 | ~~Codex 14 登记上浮三件~~ **全部完成(①③随接线批;② W1.3 清账 2026-07-26)** | ① 停靠老化调度接线 + transitionTask CAS(接线批);② 项目层配置生产加载 **已落**(`config/project.ts` 独立 project schema + 执行器认领/恢复链白名单拒收留痕 + [git].protected 并集消费;§12-9 反例 `config-project.test.ts` 6 例 + 执行链集成锚,代码 `6bc48dc`,evidence `w1-batch.md`);③ retryTask 重派发(接线批) |
| 9 | ~~场次②–④共同前置:接线增量批~~ **已完成(2026-07-25 接线批会话;代码 `4e97ae1`+`5769ae9`+评审回修批,evidence `e2e/evidence/wiring-batch.md`)** | 六项全落:① SessionManager live 构造(`cfg.privacy.store_transcript` 已传,G6 锚)+ unheard 过滤 + 转写落盘 `~/.saydo/sessions/`;② tier1 操作面(Brain 工具面 + console POST 写口 + TaskDetail 操作行;owner 触发动作已回填 session-2 step5/6);③ live 工具调用环(function-call 装配 + dispatch 确认词表环)+ Context Pack live 传入 + 热词偏置传参(asr.hotwords);④ 停靠老化调度挂生产(30s 步界 + 72h 老化 + package 回落 draft)+ transitionTask CAS;⑤ console api.ts 同源化 + vite proxy(47120 dev 路径 Playwright 覆盖);⑥ retryTask 重派发(failed→queued (U),走 canTransitionTask)。**边界已解除:Tier1 生产执行器批已完成**(2026-07-25;代码 `6e9014d`+`b837fdc`+`b9412aa`,evidence `e2e/evidence/executor-batch.md` / conformance `tier1-conformance.md` / 真 agent `e2e/poc/tier1-live-executor/`)——认领 queued 起 cursor-agent 进程 + S2 审批门 live 上浮 + settle 回叫链全落,场次②的 step 2-4 已就绪 |
| 10 | ~~TTS 音色听感拍板~~ **已拍板；源码缺省已落地，live 显式覆盖尚未切换(owner 2026-07-26 拍板 tianmeiyueyue;W5a 3.0 落地 2026-07-27)** | 源码缺省音色已切 `zh_female_tianmeiyueyue_uranus_bigtts`(代码 `ee5a366`,真实合成烟测 exit 0)，但 2026-07-31 现场复核的 live `~/.saydo/config.toml` 仍显式覆盖 `zh_female_jitangmei_uranus_bigtts`；切换 live 配置须由 owner 另行决定，当前实例不得写成已切。候选样本留 `~/.saydo/tts-voice-candidates/` 供回溯。分句策略首包已覆盖核实(spike synth.mjs 每句独立连接,p50=202ms/p90=382ms,07 D5 500ms SLO 达标——引 phase-1.md 证据免补测) |
| 11 | ~~Codex 20 triage 移交五件~~ **全部完成(W2 阶段 0,2026-07-26;代码 `5391b0f`)** | ① A1 补偿控制(gate 请求处 digest 校验 + 漂移终止活跃 run + 自愈重写);② `validateTier1Config()` + §12-9 反例 13 例;③ 非 cursor 后端启动即拒 + steerApplied 恒 queued_delta;④ gate.sh `jq -e` 输入校验 + 三反例;⑤ step_confirm 承载核对(结论落 evidence §0-⑤,canonical deferred 注已回写 09 §9/§13)。evidence `pull-forward-batch.md` §0 |
| 12 | **T2 组网与手机烟测(W2-B 代码已收口)** | **2026-08-21 复核:**Network Extension 已 `[activated enabled]`;Tailscale `BackendState=Running`、`Self.Online=true`。`[t2].tailnet_hosts` 与 `listen=0.0.0.0` 已于 2026-08-21 00:20 随既有 runtime 重启生效(无需为此 deploy 新 SHA)。剩余是 `just t2-pair` + 手机四页烟测(Dashboard/任务/审批/review,11 §4 `<768` 断点)+ 批一条 S2 + 点 S3 合并按钮见拒绝话术。**结论只适用于常驻 `ada7981c`(无移动壳 / 无 T19 首启门),不得外推 HEAD。** |
| 13 | **W4 owner 触点(代码/自动化全绿,待真人；2026-07-30 现场刷新)** | ① **S3 卡真人过卡**(§4-②):console 任务详情"用 Touch ID 批准合并"——须经 `http://localhost:<port>` 打开(rpId 绑定;127.0.0.1 会 308 归一)、首次先"注册批准指纹"(一次性 bootstrap),再过卡合并;自动化全链已证机制(`s3-merge-chain.test.ts` 32 例真验签)。② **OctoBlog 首篇文章逐节验收**(§4-③):有效配置已是 `enabled_project_types=["coding","writing"]`，无需再次翻值；生产库尚无 writing 项目或 task，真人全链仍待约。③ **runtime 部署门(2026-07-30 当时):**runtime clean @ `b20151440011ce0452417439c2d81745cb5d7d39`，双进程 loaded SHA、provider 探活、数据库与 voice handshake 已核对。**2026-08-21 当前:**常驻已是 `ada7981c`;场次①已在该树上 `failed`，返工已部署，待 owner 从步骤 1 复验——不是「场次尚未开始」。 |
| 14 | **RA-closeout 收口(2026-07-28)** | pending 裁决 (a) 案:pending 首包可出、拍板/派发拒(`project_pending_promotion`),**promoteProject 已注册 Brain 工具**(转正链生产触发点);A2 挑战绑定固化(v12:attempt/revision 签发固化+漂移即废;register 强制可审计 intent);**迁移框架 FK 合规**(父表重建带引用行老库不再升级即砖);tailnet 屏幕批 S2 收据落 push/paired_device_pin 对表;proposed_ttl_hours 全局键。evidence `e2e/evidence/ra-closeout-batch.md`;canonical 同批同步 |
| 15 | **A3-armed 收口(2026-07-28；产品语义已由 owner 于 2026-07-30 确认)** | 就绪门证据绑定全链(Codex 22 A3 关闭条件):remember 带 readinessKey 过**来源完整性四闸**(词表/转写锚+audit 指纹/trust 只可 user_stated/projectId 会话自取)落候选 → **confirmReadiness 复述确认环**(daemon 机械渲染,人在环)升格 → **ReadinessBinding 一等实体**(v13;claimDigest 换证可测/snapshot 链/receipt/knowledge 轴 foundation_generation 换代失效)→ covered = 现役 confirmed key 集(candidate 不算);**双 digest 版本锚**(checklist/evidence 入 readinessRef 与包 digest)+ **dispatch 消费事务内权威现势复核**(readiness_stale,收据 voided_by_conflict 终态);**门拒绝集收窄 isReadinessBlocking**(`gap_critical` 拒，`gap_knowledge/gap_requirement` 仅提示并放行；owner 已确认);**生产恒 armed**(删 null 旁路与空回退,无 unarmed 回退);pending 最小清单(type_intent+rough_goal)= "proposed 起 ref 必填"无例外;instructions 采访段清单单源生成。基线 contracts 73 + daemon 679\|4 skipped;§12-15 反例 22 例;批末 review 1A/2B 全修。**armed 激活冲击**:OctoDesk(唯一 active)零绑定 ⇒ 新提议需引导重绑(10 #45);2026-07-29 生产库仍为 active binding 0，场次②运行册已补复述确认前置。evidence `e2e/evidence/a3-armed-batch.md` |
| 16 | **remote-mobile-w0 LAN 进壳第 0 步(2026-08-16 已收口,代码 `addfd19`)** | A1 精确码直挂移动树 + A2 重连单一 owner + 09/11 回写 + RFC1918 出码。`just ci` 双矩阵绿;定向 Playwright 6/6;Codex 74 B 已吸收。不部署常驻。evidence `e2e/evidence/remote-mobile-w0.md` |
| 17 | **T19 × tailnet 前置门** | HEAD 对 tailnet setup probe 返回 `setup_local_only`，console 只旁路 `mobile_lan_route_rejected`。owner 拍板并先回写 09 / 11 之前，禁止升常驻、禁止加 `SAYDO_MOBILE_LAN`。不得把 `setup_local_only` 静默并入 `remote-mobile`。 |
| 18 | **[warn] 定时快照备份** | 自 2026-08-07 起定时快照连续因 `workspace_identity_changed` 失败；最后成功为 `20260806T013953Z`。来源是 2026-08-21 对本机 `~/.saydo/logs` 与 `~/.saydo/backups` 的现场取证；仓内没有可复跑的原始备份日志，只保存本结论。该修复是下一次部署门前置，但本批不修。 |

## 3. 环境状态(就绪;收口会话核实)

- Node 22 / pnpm 10 / Python 3.12+ / uv / sqlite3 / jq / just / headless Chrome + Playwright 均可用。
- `~/.saydo/config.toml`(dev profile)+ `~/.saydo/.env`(OPENROUTER / DOUBAO_TTS / NTFY / **VOLC_APP_ID+VOLC_ACCESS_TOKEN(ASR 三元组,已可用)** 已填;OPENAI/ANTHROPIC 空)+ `SAYDO_DEV=1`(~/.zshrc)。
- **hopper-dist 已落地并切锁**:`~/.saydo/hopper-dist` @ `bdd1e548`(baseline.2,已 build)+ `~/.saydo/hopper-vault`(专用 vault);bridge 启动 capabilities 断言对锁定点。
- BYOA CLI:cursor-agent 订阅态可用(2026-07-25 15:57 复核登录正常;Codex 面板报的 keychain -50 为其只读沙箱伪证)/ codex ChatGPT 登录可用 / `claude` CLI 2.1.225 已用 Claude.ai 登录(Max 订阅,零 API key;2026-08-21 复核)。
- 源码的 A3-armed 已收口不等于本机 deep readiness 已武装；当前常驻 evaluator 为 `cursor_cli`，启动日志仍有 unresolved warning(`resolveApiProvider 只处理 api 形态,got cursor_cli`)。
- **tag:`v0.1.0-rc.1` 已打并推远端**(annotated @ `628f7e4`,剥离 SHA 断言过;`v0.1.0` 待场次④通过后打;返工走 rc.N+1,旧 rc 永不移动)。
- 注意:daemon 首次以新代码启动时会对既有库自动补跑 DDL v2 迁移(audit_log 不可变触发器,additive)。

## 4. 铁律速查(全文吸收,违者算 bug)

零 emoji(11 §12,勾叉符同禁;`→`/`↔` 合法;门禁 `scripts/check-emoji.sh`)· 状态词纪律(执行不说"完成";settle 后 = "执行和检查都跑完了,等你验收";合并后才"交付")· **改 DDL_V1 必配增量迁移 + v4-era fixture 回归**(W2 场次① A1 教训:M 批→W1 一路把新表新列塞进 DDL_V1 原文不配迁移,CI 全用新建库恒绿,owner 老运行库 dogfood 首日连环崩;回归锚 `test/storage-migration-v5.test.ts` + `test/fixtures/schema-v4-era.sql`,测试不得把"最新版本号"写死)· Gate 0 未关拒 dispatch 且无 bypass 分支 · S3 语音绝不放行;**S3 merge 兑现(W4,09 §3.3):Tier1 路径经 S3 屏幕审批卡(WebAuthn Touch ID)签 `S3MergeReceipt` 判别型收据,`approveMerge` 五步事务是 `review_approved_waiting_merge → merging` 唯一入口(canTransitionTask receipt-gated;generic screen 收据不构成 S3 收据,类型层+DDL CHECK 双层);无 `S3MergeReceipt` 不进 merging;未注册 passkey / owner 选人工时降级 requestManualMerge + MergeProof watcher(不删)**;Hopper 路径 merge **仍恒人工交接**(SayDo 不自动调 `hopper merge`,09 §14-A9 未裁决,**代码不得出现自动调 `hopper merge` 的路径**)· verify 只认登记模板 + 内容冻结 · TTS 必经 redactor 脱敏 · 记忆 candidate→trusted / M0 拒第三方 / forget_hard 传播 · 契约不分叉(只 import `@saydo/contracts`;实现与 09 形状冲突以 09 为准,改 09 走轻量评审,绝不静默双改)· 敏感样本运行时拼接构造 · 两提交法(feat/fix → chore(evidence),evidence 记录代码提交 SHA,hash 不自指)· 每步独立核实真实落盘/真实 SHA · **常驻 daemon 走 runtime 树**(W2 场次① C2:`just daemon deploy [sha]` 把常驻切到 ~/.saydo/runtime 独立树的收口 SHA;开发树改代码/跑测试不打断 owner 语音会话;deploy 会重启 daemon 与 pipeline——批收口后知会 owner 再跑,dogfood 时段不重启)。

## 5. 关键实测知识(校准过实现的事实,后续会话免重踩)

- cursor stream-json:model 在 `system.init` 事件顶层(非 assistant);codex `exec --json`:流内无 model(item.type=agent_message)——工程 ADR-002 的实测依据。
- Hopper baseline.2:capabilities `commit=bdd1e548…`、`settle_event=runtime`(RunSettled 可用)、`steer=none`;automation 无法自批 needs_human 任务;`RunSettled.summary_path` 是 `.md`(iframe 走受控映射 .html + emoji 呈现层转换)。
- Hopper 锁定副本手工探针(2026-07-25,p05.md §4b):`review show` 规范字段 `evidenceDigest`(camelCase)且含 `prospectiveTreeSha`;merge 冲突 = `MergeFinished{status:"conflict"}` 事件(triage 冲突无 Merge 事件——conflictSource 判别键);merge 门拒(未 approve / 无 binding / needs_human 未 waive)= `blocked_gates` 退出 1 不发事件;**acceptance=needs_human 时 approve 需 `--waive`**;fake-runner `last_run_cost` 恒 null。
- FTS5 trigram:中英混说/长词召回好;1-2 字中文查询需 LIKE 兜底(fts.search 已内置)。
- sauc 不回置信度(09 §10 confidence 已可选化);mp3 整段直发会尾截断,必须 wav/pcm;全半角标点须 unicode 归一化。
- **WAVEFORMATEXTENSIBLE 陷阱**(R45 排查,2026-07-26):afconvert 从 **m4a** 转 wav 产 WAVEFORMATEXTENSIBLE 头(fmt chunk 40 字节),sauc 整段识别对其返回 `audio_info.duration=0` + 空文本**不报错**(静默按 0 时长);mp3 源转出经典头(fmt=16)故合成底板从未踩坑。喂 sauc 前必须重打包**经典 44 字节 PCM 头**(fmt=16, tag=1)——`e2e/smoke/audio-smoke-5.py` 的 `to_classic_wav()` 已内置(W1.1 收编自 `~/.saydo/owner-audio/rerun-smoke.py`),`--profile owner` 跑真人底板。
- zod v4 `.partial()` 会削弱 `.default()`(parse 不填充)——要"缺省恒有值"用 `.prefault({})`(收口对账 #2 教训)。
- **TTS 音色与资源包绑定**(W1.6 实测,2026-07-26):`X-Api-Resource-Id: seed-tts-2.0` 只认 **uranus 系**音色(`zh_*_uranus_bigtts`);`moon_bigtts` 系(旧版语音合成大模型资源)一律报 `resource ID is mismatched with speaker related resource`——换音色先确认后缀系别,uranus 系实测有效清单见 §2-10。
- **shell 词表加固**(cmdeffect-hardening,2026-08-19):回哺源 `~/WorkSpace/dsh-approval-tiers`（`src/command.ts`、`test/command.test.mjs`、`docs/evidence/review-1-fixlist.md`、`review-2-fixlist.md`；本批施工用只读副本）；反例表 `packages/daemon/test/tier1-cmd-effect.test.ts`（PLAN §3 / dsh review-1/2 / 评审 1 返工）；evidence `e2e/evidence/cmdeffect-hardening.md`。
- **Claude CLI Tier1 / W5.4-a**(2026-08-20):spike 目录 `e2e/spikes/claude-cli-tier1/`(RESULT.md);golden fixture `packages/daemon/test/fixtures/claude-cli/2.1.220/`;终版 argv `--permission-mode default`;hooks matcher `*`;`finishPolicy=wait_exit_then_kill` N=5s。B-10prime 在 acceptEdits 下落盘红,v3.1 改 default,X2 证实 hook 超时不落盘。纯函数层未接线执行器主流程/gateServer。评审 2 返工见 evidence §9(代码末码 `54b981c`)。evidence `e2e/evidence/w54a-claude-cli.md`。
