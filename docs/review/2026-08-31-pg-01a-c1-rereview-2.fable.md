# PG-01A C1 fresh readback 2 独立实施对账报告

> 归档：本报告 supersedes 2026-08-31-pg-01a-c1-impl-readback.fable.md 对当前候选的适用性，旧候选 RED 与原报告保留。本轮原始报告为 logs/pg01a-recovery2-20260831.i5h14e/c1-rereview-2.md，13542 bytes，SHA-256=f29d95e354fc2c723a5eee9f275989e00812e1d936ffa76726d55fbda2ae4898；原始日志为同目录 c1-rereview-2.log，929458 bytes，SHA-256=fccc29c96123e2bd7343f232fb1e3c9ef382419380ad2ea148184e9d7bd248c3。除本归档说明，reviewer 报告与 manifest 原文保留。supervisor 已按当前 policy 2.3、同 I 坐标与 cycle-state 校验：exit=0、status=valid、next_action=stop_for_owner；未转写为 GREEN。

> task=`project-gap-closure`
> stage=`PG-01A/C1`
> cycle=`owner-recovery-20260831-2`
> task_mode=`review_only`
> review_scope=`step`
> review_ordinal=`2`
> verdict=`RED`
> 本轮全程只读，未修改、提交、合并或发布任何文件，未派子会话。

## TL;DR

[fail] C1 语义 readback 未通过：C1-1、C1-2、C1-9 仍失败；C1-3 的原 B3 已修复。九项验收为 `[ok] 6 / [fail] 3`。

当前有两个可复现 P1 blocker：

- B1：31 个 required active roots 的全量清查仍不完整，release 材料继续作“全部设备内”隐私承诺，官网 FAQ 继续把 `logged_in` 直接推成“任一 CLI 可开聊”。
- B2：active-claim checker 对上述候选真实变体返回 0；现有 mutation 只覆盖预选短语，不能证明这些实际残留会被拒绝。

这不是整个 PG-01A 或项目交付结论。B3、typecheck、现有 16 项 focused 日志通过，不能覆盖 B1/B2 的语义假绿。

## 一、固定坐标与输入校验

[ok] Git 身份实测：

- P：`79211f6fec5c6eb092419c1871e35d8eedc4e3e5`
- 被拒旧 I：`004b226db0b9a6fb0347d60dc790c97293d11635`
- 新 I / HEAD：`2b5517d322f062297d25806b02c4106e2ecc60e8`
- tree：`c5cae2f04ee907328b672e3099c865f1c3f83552`
- fingerprint：`e42dd19ca752c0ee2dca966fb0e915cbc9fce9375efcd3d45a53f060692f9727`
- 状态：detached HEAD，工作区 clean
- `parent(new I)=P`；P..I 只有一个提交
- `parent(old I)=P`，旧 I 与新 I 是同 P 的两个候选，不混用旧报告

真实 `git log` 摘录：

> `2b5517d322f062297d25806b02c4106e2ecc60e8 79211f6fec5c6eb092419c1871e35d8eedc4e3e5 fix(pg-01a): 收紧公开承诺并校验语料真相`

[ok] 输入摘要：

- `docs/plan/IMPL-PROMPT-PG-01A.md`：`2a74991ec2af1c511490ce330c4c8440d0d77149923be6d9f1e9a7c791a264ad`
- owner 决策：`5e123aa2236ddc8553d55f65244e98cd1a21139fc6857783db0f7993e1a5970b`
- 唯一 P2 ledger：`4a9274b9308a98c55d5152d06daedc8b6760d0fd088cdbc25cabb5a87f4fdae8`
- focused 原始日志：24122 bytes，`c37af2d284f60c61ffcc84612ca35c93c1ea15fd3d010ce3bb839dda40d4d816`
- focused summary：`d359cb7c10308f49d0efd7816a200686e4a1fc5e9905744b692f580e44c76eb5`

[ok] `validate_handoff.py` exit 0：

> `status=valid`，`contract_errors=[]`，`risk_violations=[]`，policy version 2。

[ok] evidence 前 13 行确认：

- `repair_rounds_used=1`
- `rereview_rounds_used=1`
- `C1_review_used=1`
- 本次派发已记账，不是对新 I 的既有审查。

## 二、范围与 33 路径全量核对

[ok] P..I：

- 33 个路径
- 969 insertions / 114 deletions
- C1 总 must-change 15 个全部出现
- 无总允许集合外路径

[ok] 旧 I..新 I：

- 12 个路径
- 164 insertions / 58 deletions
- recovery must-change 7 个全部出现
- 无 recovery exact-set 外路径
- 未触碰 corpus source、48 authority inputs、dry-run model、renderer、oracle 或两份投影

33 个路径已逐一纳入 diff/readback：

- 治理与 canonical：`AGENTS.md`、`HANDOFF.md`、`README.md`、`docs/06-references.md`、`docs/11-ui-spec.md`、`justfile`
- 现役网站：`deploy/saydo-octoooo-com/docs/index.html`、`en/docs/index.html`、`en/index.html`、`en/privacy/index.html`、`index.html`、`privacy/index.html`
- release 与设计稿：`docs/release/2026-08-13-app-materials.md`、`docs/release/metadata.json`、`docs/site/style-demos/11-hybrid.html`
- console：`SetupGate.tsx`、`SetupWizard.tsx`、`SupplyPicker.tsx`、`setupWizardUx.test.tsx`、`resourcePlans.test.ts`、`resourcePlans.ts`、`setupApi.ts`
- corpus：`README.md`、`check-q0-truth-report.mjs`、两份 dry-run 投影、`test-q0-truth-mutations.mjs`、`validate.mjs`
- claim checker：`scripts/check-active-claims.mjs`、`scripts/test-active-claims.mjs`
- 模板：三个 `templates/saydo.*.example*`

另外程序化完整读取了 `REQUIRED_ROOTS` 全部 31 个文件并逐个计算字节数、行数和 SHA-256，最终 `TOTAL=31`，不是抽样。

净新增绕门扫描未发现 `fromCharCode`、`eslint-disable`、`@ts-ignore`、测试 `.skip/.only`。命中的 `only` 均为普通英文正文或测试标签文本。

## 三、C1-1 至 C1-9 对账

| 验收项 | 状态 | 结论 |
|---|---|---|
| C1-1 公开声明止损 | [fail] | 多数已收紧，但 required root 中仍存在绝对设备内隐私承诺及“任一已登录 CLI 即可开聊”。 |
| C1-2 active-claim checker/mutation | [fail] | root 集合、独立分类 mutation 均存在，但 checker 漏掉候选中的真实变体，官方 gate 因此假绿。 |
| C1-3 Q0 producer/checker/mutation | [ok] | 986 coverage、八字段、排序、聚合、disposition、Git identity 的独立内存负例均被拒。原 B3 已关闭。 |
| C1-4 I/E producer 时序 | [ok] | `git ls-tree` 确认 I 中没有正式 Q0 report；未自绑定自身 SHA。 |
| C1-5 源摘要与投影 | [ok] | source digest=`0c2a1f...b2da6`，authority digest=`e62d211...ab98`；48 authority paths 在 P..I 间 changed_count=0；两份投影验证通过。 |
| C1-6 copy-only/runtime 不变 | [ok] | 无 daemon/contracts/pipeline/docs09/docs10/workflow/lockfile 变化；console 生产改动仅文案或注释，无控制流、权限、网络、布局变化。 |
| C1-7 HANDOFF 与 I 边界 | [ok] | `HANDOFF.md:49` 为 `active=PG-01A,next=none`，并声明 readback/full gate、Q0 E report 尚待执行。 |
| C1-8 上一 cycle 三文件恢复 | [ok] | 两条向导断言仍保留结构约束；source/authority digest 正确；两份投影与现役 renderer 验证通过。 |
| C1-9 B1/B2/B3 恢复 | [fail] | B3 已修；B1、B2 未闭环，因此组合验收不成立。 |

## 四、P0/P1 质量发现

### B1 · P1 · C1-1/C1-9：31-root 清查仍有现役绝对声明

可复现证据：

- `docs/release/2026-08-13-app-materials.md:59` 仍称对话、语音、事项与账本数据“全部保存在你自己的设备”。
- 同文件 `:70` 又称音频“仅在本机处理，不上传”，而 `:71` 同时承认系统可能使用联网语音识别；`docs/06 §7` 和 `docs/11 §10.3` 要求按当前配置声明外发范围。
- 同文件 `:170` 的英文 Review Notes 继续写 speech recognition “processed on-device”。
- `deploy/saydo-octoooo-com/docs/index.html:892` 写“本机有任一已登录 CLI 即可零 key 开聊”。
- `deploy/saydo-octoooo-com/en/docs/index.html:915` 写“any logged-in CLI ... means zero-key chatting”。

后两条把 `logged_in` 直接推成可调用能力，违反 `docs/06-references.md:150` 后的证据四事实以及 `docs/11-ui-spec.md:574` 的登录态禁推规则。前述 release 隐私正文不是历史归档或测试 fixture，而是 `REQUIRED_ROOTS` 中的现役 release surface。

影响：用户会据此判断隐私边界和本机 CLI 可用性，正属于 PG-01A 的公开承诺止损范围。

- `in_scope=true`
- `needs_owner_decision=false`
- `acceptance_item=C1-1/C1-9`

### B2 · P1 · C1-2/C1-9：active-claim gate 对真实残留假绿

当前候选实际运行：

> `node scripts/check-active-claims.mjs`
> exit 0
> `[ok] active claims roots=31`

但 `scripts/check-active-claims.mjs:52` 至 `:72` 只枚举有限短语。对从候选实际提取的以下文本逐条直接运行其 pattern 集，命中均为 `[]`：

- `本机有任一已登录 CLI 即可零 key 开聊`
- `全部保存在**你自己的设备**`
- `speech recognition ... processed on-device`
- `浏览器系统语音免费`
- `配 API key 立即变快`

独立负例进程因此 exit 1。合法 conditional 文案“已配置的 OpenAI 兼容对话端点……时延与费用以服务商为准”同样保持不命中，说明无需用扩大误报来修复。

`scripts/test-active-claims.mjs:96` 至 `:113` 虽然分别注入五类短语，但只覆盖预选 needle，没有使用候选中的真实中英文变体。故同 I 日志中的：

> `[ok] active-claim mutations 12 passed`

不能证明 B1 残留会红。

- `in_scope=true`
- `needs_owner_decision=false`
- `acceptance_item=C1-2/C1-9`

### B3 · 原 P1 · C1-3：已修复

[ok] `check-q0-truth-report.mjs:171` 至 `:177` 校验八字段 exact set、缺失和类型；`:200` 至 `:208` 将全部八字段逐项与生产者结果对齐。

[ok] `test-q0-truth-mutations.mjs:86` 至 `:116` 对每个字段分别执行：

- 单字段漂移
- 删除字段
- 错误类型

独立内存验证结果：

- baseline：986 objects，0 errors
- 八字段每种漂移、删除、错型均产生非零 errors
- duplicate、排序、aggregate、disposition、Git identity 均产生非零 errors

同 I 原始日志还记录 `q0 mutations 38 passed`。因此本轮不再把 B3 列为 blocker。

## 五、门禁与原始证据

| 项目 | 结果 | 摘录或归属 |
|---|---:|---|
| V2 handoff validator | 0 | `status=valid` |
| console typecheck，本轮实际运行 | 0 | `@saydo/console`，`tsc --noEmit` |
| console ESLint，本轮实际运行 | 0 | 无诊断输出 |
| active-claim checker，本轮实际运行 | 0 | `roots=31`，但被独立负例证明为语义假绿 |
| reviewer active-claim 内存负例 | 1 | 候选中的五条 B1 类变体均未命中 |
| public-text-redaction，本轮实际运行 | 0 | `pass=28 fail=0` |
| dry-run validator，本轮实际运行 | 0 | 固定 source/authority digest，rows=600 |
| Q0 纯内存负例，本轮实际运行 | 0 | 986 baseline clean；八字段及其他不变量全部能红 |
| `git diff --check` | 0 | 无输出 |
| 同 I 既有 focused runner | 0 | raw log 绑定新 I/tree，`total=16 passed=16 failed=0`，clean_before/after=true |
| console Vitest，本轮尝试 | 1 | Vite 写 `.vite-temp` 被只读 sandbox 以 EPERM 拒绝；未重试、未伪称本轮重跑成功 |
| active mutation，本轮尝试 | 1 | `mkdtemp` EPERM；同 I 原始日志可归属 `12 passed` |
| Q0 mutation，本轮尝试 | 1 | `mkdtemp` EPERM；同 I 原始日志可归属 `38 passed` |

同 I 日志还记录 console `33 passed`、`279 passed`。这些是绑定同一 HEAD/tree 的已存在原始输出，不冒充本轮重新执行的 Vitest。

## 六、Deferred P2 ledger delta

本轮没有独立 P2 发现，也未修改唯一 ledger。

```json
[]
```

## 七、未运行清单

按派发边界未运行：

- `just ci`
- 页面 headless Chrome 渲染
- 正式 Q0 `--write` 与正式 report `--check`
- week-audit writer、E evidence commit
- 真实账号、live AI、connector、发布、设备、签名与商店测试
- push、merge、deploy

正式 Q0 report 在 I 中缺失是合同要求，不是漏交。

## 八、修复清单与返工去向

1. [P1/B1] 收紧 `docs/release/2026-08-13-app-materials.md` 的设备内处理总括承诺，并处理官网 FAQ 中“任一 logged-in CLI 即可”的推断；随后重新全量扫描同一 31-root 集合。
2. [P1/B2] 让 checker 覆盖候选中实际存在的中英文及 markup 变体，并将这些真实变体分别加入独立 mutation；保留 conditional/preview、带“约”的观察时延和“零 key 不等于免费”等合法反例。
3. [ok/B3] 无需返工。

两项 blocker 都在既有 C1 exact scope 内，不需要产品取舍。返工或停止由 supervisor 根据已核验 cycle-state 与 review-manifest validator 决定；本 reviewer 不授权额外轮次，`stop_reason` 不因 ordinal=2 自动填写。

```review-manifest
{
  "verdict": "RED",
  "review_ordinal": 2,
  "candidate_head": "2b5517d322f062297d25806b02c4106e2ecc60e8",
  "diff_fingerprint": "e42dd19ca752c0ee2dca966fb0e915cbc9fce9375efcd3d45a53f060692f9727",
  "review_scope": "step",
  "blockers": [
    {
      "id": "B1",
      "severity": "P1",
      "summary": "31 个 required active roots 的公开声明清查仍有绝对设备内隐私承诺，并把 logged_in 直接写成任一 CLI 可用",
      "evidence": "docs/release/2026-08-13-app-materials.md:59; deploy/saydo-octoooo-com/docs/index.html:892; deploy/saydo-octoooo-com/en/docs/index.html:915",
      "in_scope": true,
      "needs_owner_decision": false,
      "acceptance_item": "C1-1/C1-9"
    },
    {
      "id": "B2",
      "severity": "P1",
      "summary": "active-claim checker 对候选中的真实禁用变体返回假绿，现有 mutation 未覆盖这些变体",
      "evidence": "scripts/check-active-claims.mjs:52; scripts/test-active-claims.mjs:96; docs/release/2026-08-13-app-materials.md:59",
      "in_scope": true,
      "needs_owner_decision": false,
      "acceptance_item": "C1-2/C1-9"
    }
  ],
  "p2_ledger_delta": [],
  "focused_gates": [
    {
      "name": "same-I-focused-log-16",
      "exit_code": 0,
      "summary": "绑定新 I/tree、clean_before/after=true，16 项记录均 exit 0；active checker 的语义假绿由本报告单列 blocker"
    },
    {
      "name": "console-typecheck",
      "exit_code": 0,
      "summary": "@saydo/console tsc --noEmit 通过"
    },
    {
      "name": "console-eslint",
      "exit_code": 0,
      "summary": "packages/console/src lint 通过"
    },
    {
      "name": "active-claims-check",
      "exit_code": 0,
      "summary": "报告 roots=31，但独立实际变体负例证明该结果是假绿"
    },
    {
      "name": "reviewer-active-claim-negatives",
      "exit_code": 1,
      "summary": "五条从候选提取的 B1 类变体均未命中 checker pattern"
    },
    {
      "name": "q0-in-memory-mutations",
      "exit_code": 0,
      "summary": "986 基线零错误；八字段漂移、删除、错型及排序、聚合、disposition、Git identity 负例均被拒"
    }
  ],
  "stop_reason": null
}
```
