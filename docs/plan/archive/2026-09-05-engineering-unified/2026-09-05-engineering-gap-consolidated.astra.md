# SayDo 工程缺口综合方案 · Astra

日期：2026-09-05。状态：供 owner 选择的综合方案；本文件不是第二排产源，本轮不实施产品。

本次修订落实后续回报与成本讨论：保留核心修复，新增能力按明确收益与持续成本选择；不要求完整实施全部候选。

输入全文：[Fable 扫描](2026-09-05-engineering-gap-scan.fable.md)（233 行）、[Astra 原报告](../review/2026-09-05-project-gaps-Astra.md)（194 行）。基线为本轮 `git log` 实读的 `bcf8ea855f25b177888d9159f75e49214f3dd892`；两稿及其摘要已冻结于[来源清单](../../research/astra-gap-synthesis/source-manifest.json)。以下引用均以该基线为准。本方案保留原任务的排除范围，不新增外部用户使用或反馈工作。

## 1. 谁的方案更好

**作为实施底稿，Astra 原稿更可靠；作为寻找遗漏与挑战投入方向的扫描，Fable 有值得吸收的增量。新稿以 Astra 的证据和边界为骨架，补入 Fable 的运行形态与交付收益视角。**

| 比较维度 | Fable | Astra 原稿 | 本次取舍 |
|---|---|---|---|
| 正式路径的事实准确性 | 找到了非流式与两种启动形态，但误报延迟零接线、CLI 两命令、控制面零推送 | 生产路径加探针更扎实，区分未知值、后台失效、部分失败 | 使用复核后的代码事实，不照搬任何一稿结论 |
| 投入优先级 | 主动追问何时产生产品收益；但把安全与动作正确性近似成无产品收益 | UI、凭据、审计、恢复优先有依据；对新能力何时进入过于保守 | 继续当前止损，明确下一项可提请排产的观测小批 |
| 实施粒度 | 将流式、gist、provider 抽象捆绑；将升级与诊断捆绑 | 最小切片较清楚，但运行形态差异漏项 | 分开观测、音频、LLM、页面更新和分发生命周期 |
| 风险与合同 | “全部 L1、零 09 变更、零评审成本”不成立 | 保留跨层红线、条件包与排产约束 | 分级取决于触达的边界；不以行数和“仅配置”降级 |
| 证据强度 | 自查修正了一些计数，但关键错误仍在 | 有独立复审；合成探针未冒充实测 | 保留可复跑证据，不作速度、收益或工期保证 |

我的原稿也有两项需要改进：一是没有把常驻源码执行与发布包执行放到同一张维护清单；二是缺少可直接交接的首批合同。两稿都没有把“完整输出校验与逐句早播之间的冲突”展开到足够可实施的程度，本稿补齐。

## 2. 必须先纠正的关键判断

### 2.1 延迟已有接线，问题是口径与完整性

`packages/daemon/src/index.ts:2211` 创建 `LatencyCollector`，`:2901` 接模型回调，`:3055–3066` 接阶段事件与 playout，`:927–929` 有受开发入口保护的报告。`pipeline/src/saydo_pipeline/hub_client.py:457–463,507–511` 发阶段事件。Fable 按字段名搜不到调用，不能推出“生产从未产出 trace”或“两个月零接线”。本轮没有读取运行中的日志，不能保证当前实例确实积累了有效样本。

真正的问题是 `obs/latency.ts:123–133` 的 `passPublish` 只判断 P50；完整响应和整句合成被用作首 token/首字节近似；partial 没有明确有界回收。本轮内存探针得到 `n=20,p50=1000,p90=10000,passPublish=true`。这是统计缺陷的合成复现，不是发布流水线或真实延迟结论。

### 2.2 控制面有部分推送，正式看板仍缺刷新闭环

`voice/hub.ts:813–829` 已下发 `confirm.card/countdown/resolved` 与 `focus.entity`；`shell/Layout.tsx:326–334` 消费实体变化刷新侧栏。Fable 的“推送通道只有语音、审批卡完全等轮询”过于绝对；语音确认卡和 REST 审批列表也不能混为同一对象。

正式看板由 `App.tsx:48–49` 进入 redesign；`useBoardPageData.ts:49–69,99–100,150` 仍为 N+2 读取、详情失败跳项、手动 tick 更新。旧 `Board.tsx` 的 30 秒轮询不能为它提供刷新上限。已有探针复现 20 项对应 22 请求、两秒内零自动刷新、单项失败静默变 19 项。两秒仅是观察窗口，不是“永远不刷新”的动态证明，结论还依赖 effect 与调用点。

### 2.3 新事件和音频分块不能承诺零契约变更

`packages/contracts/src/types/pipeline.ts:17` 是封闭判别联合，hub 收发均经过 schema；本轮 `safeParse({t:"state.changed",...})` 返回失败。新增消息即使只有 ID 和 revision，也要更新 `docs/09`、共享 schema、方向/权限、兼容与重连语义。不能为避免合同变更而绕过 schema。

当前 pipeline 把完整句 MP3 放进一帧（`hub_client.py:513–517`），Console 用每个音频 URL 创建 `Audio` 并按句排队（`useVoiceChannel.ts:330–344`）。把供应方 chunk 原样转发，尚未证明客户端能连续播放；必须确定音频编码、序号、句尾、取消和 watermark 如何对应。不能把网络分块等同于播放器可用的音频段。

### 2.4 两种运行形态成立，“只有公开用户会撞包问题”不成立

`launchd/cli.ts:83–101` 使用 tsx 与源码入口；deploy 使用独立锁定 runtime 树，并不是开发工作树随改随跑。`packages/cli/src/supervisor.ts` 使用打包 daemon，`scripts/build.mjs` 负责产物。`HANDOFF.md:61` 只证明历史记录称常驻未升级，本轮未探活。

仓内已有 `verify-distribution.mjs:143–173` 的打包安装验证及 CI 三平台 distribution job。应补两种运行形态的差异覆盖，不应抹掉现有包验证。即使 daemon bundle 相同，pipeline、配置、依赖、平台和状态根不同，仍不能宣称“狗粮等于整个交付物”。

### 2.5 交付节奏值得审视，行数比例不能决定裁决

本轮 `git log --since=2026-08-28` 确认 20 个提交，其中 `b46daed144e103016e1e4765ea521ebcb8694291` 引入一条命令安装与镜像回退。若只计核心任务执行能力，增量可能很少；将分发收益排除后再断言“没有任何可感知变化”不合理。PG-01B 的放弃/归档、预算 unknown 和 PG-02 的可操作状态也直接影响产品。

7.6 倍是 Fable 按路径分类的增删行比，本轮没有复刻其完整分类口径；它不是工时、净代码量或价值比。文档迁移、生成账本、重复修订都会影响 numstat。值得保留的是“每批必须说明可观察的改善”，不采用按 checker 数量或文档行数裁掉必要验证的办法。

## 3. Fable 全文逐项取舍

“保留”指建议方向有价值，不等于当前已授权。表内路径省略 daemon/console 前缀时，按 §2 或原报告完整路径定位。重复出现的建议合并到同一实施候选，不累加成额外批次。

| ID | Fable 原点 | 判断与理由 | 新方案落点 |
|---|---|---|---|
| F00 | §0 七条主缺口与总排序 | 修正：语音性能和运行形态重要；零接线、零推送及总工期失真；未覆盖安全收益不能推翻原优先级 | §2、§4 |
| F01 | §1.1 ASR/LLM/TTS 批处理、过渡语、流式 | 保留非流式判断；PTT 完整音频识别本身不要求马上改。系统可在非流式模型运行中发真实进度提示；“只能模型返回后提示”不成立。亚秒没有证据 | VOBS-01、VOICE-01/02；工具阶段单列 |
| F02 | §1.2 常驻统一 bundle | 保留差异治理，先最小身份诊断；分发变更或已复现身份差异再触发隔离同包验证。正式迁移触及部署/身份/数据恢复，不是 L1 的改启动行 | HOST-01/02 |
| F03 | §1.3 Pipecat、RMS VAD、单厂商、provider 接口 | 未启用 Pipecat 与源码 RMS VAD 有依据；工程 ADR 已记偏离。接口可随具体流式边界引入，不能说流式必须先建通用 provider 层 | VOICE-01；依赖清理有条件附带 |
| F04 | §1.4 双页面树、自制 router、DTO、787 KB | 新旧树并存不等于旧树可删；自制 router/没用请求库不是缺陷。按现役行为迁移与共享边界处理，未测得 bundle 卡顿不做拆包批 | PG-01B/02、VIEW-01/02 |
| F05 | §1.5 index/executor 只减不增、禁止加 mutation | 保留增量模块化；拒绝硬行数门，避免代码搬家或压行“合规”。必要接线可留组合根，新业务逻辑进入既有域模块 | 触达时约束，不新增 checker |
| F06 | §1.6 草案归档或漂移检查 | PLAN-2 已为 AI-DRAFT 标 `archive_deferred`，草案 README 已声明不构建、编译不代表正确。无需因体积再次搬迁；漂移检查只在选定下沉切片有消费目标时做 | 保持 deferred，PG-02 只取 exact safety types |
| F07 | §2.1 采样零接线、加 JSONL/value-report | 修正为已有 collector、日志与 dev 报告；先修统计与观测链，不新建重复端点，不往不可变审计塞性能原文 | VOBS-01 |
| F08 | §2.2 12 轮窗口与廉价模型 gist | `slice(-12)` 是历史数组元素窗口，不可不加区分地称 12 个完整往返。约束遗失需定向复现；gist 增加延迟、成本、失真与信任边界，不能随流式顺手加 | CONTEXT-01 候选；先查漏检 |
| F09 | §2.3 测试规模、waitFor、门禁总量、拆测试文件 | 行数和 waitFor 数不是耗时归因。先取 suite 耗时/稳定性；拆文件可能改变隔离或并发，不能仅称搬运。按已有预算避免重复 full gate | 维护原则，实测热点才开小修 |
| F10 | §2.4 前端 chunk、奠基进度 | chunk 数值来自本地构建产物，不是加载性能。奠基进度保留记录，不扩为本次工作 | 不排新批 |
| F11 | §3.1 Brain 内向与执行 agent 外部工具 | 保留两层区分；外部 agent 的 egress 风险沿 G-B2，不称“后门”来暗示已证实绕权漏洞。Brain 仍缺可验证资料链 | READ-01，执行沙箱保持既有边界 |
| F12 | §3.2 HTTP/CLI、本地模型 conditional | 保留不扩厂商；OpenAI-compatible localhost 配置是否可用还要过模型身份、工具、错误与准入，不能靠 family 前缀和一条 happy path 升级支持状态 | PG-06 后按具名本地组合另批 |
| F13 | §3.3 单语音厂商、macOS say | 保留可用性缺口；终端 say spike 不等于浏览器/手机出声，也不自动满足取消/脱敏/watermark。不要承诺“永远发得出声” | 先显式 degraded；备用 TTS 独立候选 |
| F14 | §3.4 Linux 桌面通知 | 保留为已有范围差异，不因此扩大跨平台承诺 | 现有 G-B6/B12 条件批 |
| F15 | §3.5 CLI 生命周期 | 修正为 `up/status/open`（纯解析探针已验）。doctor 值得做；upgrade 依赖兼容、恢复、版本验证与发布条件，不能与只读诊断同级 | HOST-01；upgrade 后置到 HOST-02 条件 |
| F16 | §4.1 等待反馈与 UI 零成本 | 保留真实状态反馈；上屏、出声及模型原文有不同投递/脱敏规则，非零成本。不可先播“已落账”再核对工具结果 | VOICE-02；保留输出闸 |
| F17 | §4.2 10/30 秒轮询、state.changed | 修正正式路由、已有事件、schema。先补有界刷新与失败保留；是否新推送通道由后续读模型合同决定 | VIEW-01/02 |
| F18 | §4.3 PTT/说完了/重听热键 | 收窄：先修真实 TaskModal 焦点圈、Escape、返回焦点。全局/可配置快捷键要验证输入框冲突、按键释放、平台与误录音 | A11Y-01；热键暂候选 |
| F19 | §4.4 G-A3、首启供给、locale | 保留去重，已有编号不另开同义批 | PG-01B/02、原 G-B4/B7 |
| F20 | §5.1 规模、7.6、20 提交、评审回合 | 数量作为描述，不作为自动裁撤质量门的证据；20 提交已复核，7.6 未复刻完整口径，单批样本不是稳定产能估计 | §2.5、§6 |
| F21 | §5.2 安全链无产品收益 | 不采用绝对结论；止损、正确动作与恢复都是收益。接受需要明确下一项能力候选，不能让语音问题只留历史文字 | §4 提名 VOBS-01 |
| F22 | §5.3.1 checker/evidence 硬上限 | 不采用硬数量上限；不新造通用 checker，复用原测试；重门禁只在最终候选及相关变化后运行 | §6 |
| F23 | §5.3.2 插 VOICE/RUNTIME 两大批、全 L1 | 收窄为一项可独立判断的 VOBS-01 提名；流式与升级风险另分。当前消息没有授权改排产 | §4、§5 |
| F24 | §5.3.3 合并 audit/db | 不采用。不可变审计与生产迁移有不同红线、回滚及故障面；目录相邻不足以证明合批回报 | PG-04/05 各自保留 |
| F25 | §5.3.4 草案/Pipecat 减法零风险零评审 | 不采用零成本断言；先确认依赖闭包、许可与安装测试、canonical 状态。保持一名独立 reviewer 的既定制度 | 仅触达时机械清理 |
| F26 | §6 已有 G-A/G-B 去重 | 保留 G-A2/3/5/6/7/8/9 与 G-B2/3/6/13/14/15/16 的原归属；日志吞吐不能塞 PG-04 | 原链与条件包 |
| F27 | §7 八项顺序和 8–11 天 | 第一项改为观测修复；第2项依赖/归档不先做；第3项拆音频/LLM并按瓶颈选一项、排除gist；第4/5项先被动诊断，有分发变更再验证同包与切换；第6项先减少无效刷新；第7项候选；第8项保留 owner 决策 | 不累计粗估为承诺 |
| F28 | §8 自查与修订记录 | 肯定其删除无界历史等错误的修订；仍不能替代独立取证。自查次数、门禁 clean 不证明建议事实准确 | 本轮独立审查新方案与 prompt |

Astra 原稿的主要方向也全部处置如下，避免新稿只纠正 Fable：

| ID | Astra 原点 | 本次处置 |
|---|---|---|
| A01 | §2 动作、预算、真实写口 | 保留；明确首批默认 PG-01B，不能把真实 TaskModal 一并禁用 |
| A02 | §3 页面更新、N+2、业务读模型 | 保留；拆成 VIEW-01 既有读口刷新与 VIEW-02 聚合/共享合同，先少改一层 |
| A03 | §4.1 凭据、准入、审计 | 保留原 PG 链，不因新性能建议延后已经登记的止损 |
| A04 | §4.2 迁移前恢复点 | 保留，并作为任何自动升级/常驻切换的必要前置 |
| A05 | §5 观测与单路径流式 | 补生产接线反证；将音频分块与 LLM 早播分开，补全输出闸与播放端合同 |
| A06 | §6 日志/容量测量 | 保留条件式测量；20/100 Focus 是工程 fixture 规模，不是支持承诺 |
| A07 | §7 外部资料链 | 保留一个只读来源先闭环；SaaS、付费、写能力均保留 SP5/SP6 条件 |
| A08 | §8 诊断与分发 | 吸收 Fable 的双运行形态；最小被动诊断可提前，同包验证与正式迁移按具名分发需求启动 |
| A09 | §8 TaskModal 键盘 | 保留真实组件定向验证，优先于全局热键系统 |
| A10 | §9 不投入方向 | 保留：不换数据库、不造连接器平台、不恢复双执行路线、不按测试数量计功 |
| A11 | §10–11 证据与独立复审 | 保留合成/真实、组件/E2E、报告/产品 GREEN 的边界；本轮另审新增方案和 prompt |

## 4. 推荐投入顺序与排产关系

**建议保留核心修复，不完整实施所有新增候选。下一会话默认先执行当前 PLAN-2 的 PG-01B。** 它会直接修复动作/预算错误并关掉远程不安全业务面。之后原 PG-02 关闭动作/能力真相；凭据、准入、审计与恢复修复沿原批推进。这些方向通常不要求新增常驻服务或模型调用，但仍需维护权限规则、兼容行为及已有日志/恢复点的容量。新增看板读模型、流式或诊断不装进原批卡。

**远程关闭有明确的用户成本：手机或远程浏览器的业务查看和操作会不可用，包括只读业务数据；本地正常入口仍须可用。** PG-01B 要明确显示受限状态和已有本地替代入口，不能仅留下空白或失败重试。关闭是止损，不是远程产品需求的终点。若远程是 owner 当前核心使用方式，应优先提出安全重开范围和排产取舍；它仍须满足[缺口决策表](2026-08-28-project-gap-owner-decisions.md)的 D6/D18、相应 ADR、加密认证传输与信任根、逐设备最小权限和撤销、凭据存储/轮转及旧客户端恢复条件。重配对、重新登录或兼容中断由重开批明确告知。这里的 D18 是缺口决策表的 remote trust stack，不是技术选型文档的同号模型供给决策；不能以功能损失为由保留不安全远程读口，也不预先承诺重开时间。

现行唯一串行链仍是 `PROC-01 → PG-01B → PG-02 → PG-03 → PG-04 → PG-05 → PG-06 → owner-stop`。本次不修改 PLAN-2、D17、指针或批卡。PG-04 与 PG-05 分开。现行批次的 exact-set、gate、两提交法和 owner checkpoint 均沿用原合同。

**新增投入优先提名 VOBS-01：可考虑在 PG-02 后安排，仅修现有语音观测的准确性。** 它有现成可复现缺陷、范围较小，用来决定后续语音优化是否值得做。HOST-01 的最小被动诊断也可较早提名，不必等到升级；只聚合已有状态与失败信息。任何插批或远程重开优先级调整，都须 owner 具名选择后同步 PLAN-2、受影响引用、链断言与相关测试，并完成必要的独立方案 review。本次修订授权不等于批准这些排产变更；未选中时按原链继续。

安全链之外，优先考虑 VOBS-01、低开销的 VIEW-01 和最小 HOST-01。语音先据分段证据选择收益足够的一处瓶颈，不固定 TTS 先于 LLM，也不默认两项都做；若证据不足或安全改动不能带来足够回报，就停在可信观测。VIEW-02、READ-01、HOST-02、CONTEXT-01、额外供应方与本地模型退出近期默认交付范围，仅在各自条件成立时提请排产。这是选择规则，不是第二套 active/next。

## 5. 新增候选的最小合同

以下是可供新会话选择的决策卡。只有 VOBS-01 给出较完整的技术出口；其余若被选中，先补所涉 canonical 与 exact pathset、现役 gate argv，再交 Grok，不能凭本表直接扩写全部功能。

每个实际开批的执行卡只补四行，原 PG 批也适用：**用户可见回报**；**增量运行成本与上限**（请求、存储、模型用量、后台资源，无新增则写明依据）；**用户负担**（功能损失、配置、重登录或中断）；**后续维护责任与停止/扩展条件**。上限可暂待开批取数，冻结验收前须能判断；未知成本不能写成零成本。记录放进原批卡/执行副本和收口说明，不新增报表平台、通用 checker 或独立审批流程。

### VOBS-01：现有语音观测准确性，预计 L2

范围：复用 `LatencyCollector`、现有 dev 报告与普通日志；不新增公开 API、WS 消息或自动 provider 探测。不改实时部署。

回报与持续成本：得到可用于选优化方向的分段与失败报告，用户此时未必感觉更快。只增加有界内存统计和必要的聚合日志；分组维度、计数窗口、已结算轮去重索引都须有界，不能把 turn/session ID 变成永久指标标签。沿用普通日志现有轮转，开批明确本批新增字段的输出频率、保留容量/期限和维护位置；日志可轮转不等于可裁剪不可变审计。默认不留新增逐轮原文、不引入外部监控服务或模型费用；若现有观测已足够支持选型，到此停止扩建。

验收：

1. 至少 20 个有效完整样本后才允许表达 SLO 可判定；P50 ≤ 1500 ms 且 P90 ≤ 2500 ms 同时满足才 pass。少样本、非有限/逆序时间戳不能给 pass。
2. 按会话/轮次关联，重复/迟到事件不复活已结算轮；pending 设固定容量与 TTL，溢出/超时明确计数。首次实现可采用 500 条 pending、120 秒 TTL 的工程限制；写入该批验收合同后固定测试，不作为产品速度承诺。
3. 分母至少区分已开始、完整、取消、超时/缺段；文本轮、PTT、免手、工具轮不能不加标注混成一个语音首响分布。计数总量可对账；不能只让成功轮进入可见数据。
4. 现有阶段名若继续保留，报告明确 `llm_first_token`/`tts_first_byte` 当前是响应/句合成完成的近似，daemon 接收时间是跨进程代理。发现必须改 WS 词表时，先回 canonical 并重新冻结该批范围，不能偷加字段。
5. 用合成成功、P90 超限、取消、缺段、重复事件验证报告；现有生产 hub/dialog/pipeline 路径分别验证，不仅直接调用 collector。真实供应方基线属于后续具名环境的条件证据，未跑即 `not_run`，不为它读取 owner 配置或凭据。

出口：可信的分段报告及失败分母；不承诺性能达标。最低 focused 面为 collector 统计测试、voice-hub/live-dialog 接线测试和受影响 Python 测试。源树当前测试文件名由开批搜索核验，新增 collector 测试标 `[new]`，不能假装已有测试路径。

### VIEW-01/02：先让页面知道变化，再减小读放大

VIEW-01（L2）只改正式页面已有读口的生命周期：初次快照后，优先在动作成功、已覆盖该变化的现有事件、回到前台/恢复连接时刷新；事件合并、请求去重、组件卸载取消，局部失败保留事项并可重试。不把 attention 颜色翻译为确定业务状态；无可靠任务读口时显示未知或收窄标签。先列明现有事件覆盖与遗漏，不能假定已有事件覆盖全部后台变化。

不预设每 5 秒全量拉取，也不承诺统一 6 秒更新。未被事件覆盖的变化保留有界兜底刷新和手动重试；后台标签页暂停非必要请求，恢复前台重新取快照。开批将可接受的新鲜度上限与每个活跃页面的请求预算一起冻结，用指定 Focus 数、事件突发、离线恢复和多标签场景验证。当前 N+2 下，一次全量刷新 20 项会发 22 个请求；频率乘请求数只是负载估算，不是 CPU/数据库瓶颈实测。若预算与新鲜度不能同时满足，缩小刷新范围或提请 VIEW-02，不能暗中加频率。

验收同时覆盖动作成功后的定向失效、缺事件时的兜底更新、刷新不重叠及晚到旧响应不覆盖新状态；断网时保留旧内容并标 stale，恢复后在冻结的上限内更新；一个 detail 失败保留原项/占位错误；重复挂载卸载后无残留请求或定时器。收益是少手动刷新且不静默丢项；持续成本是浏览器流量、daemon 读取与电量，用户仍会看到明确的短暂旧数据状态。维护点留在现有数据 hook，不另建常驻同步层。

VIEW-01 不声称解决 N+2。VIEW-02（跨边界 DTO/权限按 L3）另补分页、有容量上限的聚合读口及共享 runtime parse，消除逐项扇出并给出业务状态来源；事件只是失效提示，丢失/重连后重新拉快照，授权 scope 不能靠事件 ID 猜。选择 WS additive 消息时同步 09、schema、角色白名单与兼容；不需要引入独立 CQRS 平台。

VIEW-02 仅在上述预算/新鲜度约束无法满足，或已有读口无法给出必要的真实状态时启动；需承担服务端读模型、分页与权限合同的持续维护，不因 N+2 这个形状本身就开工。

### VOICE-01/02：音频早播与 LLM 早播分开，均按 L3 准备

两个 ID 表示不同改动面，不表示先后顺序。VOBS-01 后先比较同一模式/配置下的阶段占比、端到端 P50/P90 和失败/取消情况；当前代理阶段只能支持其实际口径内的归因。选一条有安全可行性且预期收益足够的路径，必要时先补最小测点。主耗时若在 LLM，不能默认先做收益很小的 TTS 改动；未授权真实供应方调用时只报告 fixture 结论，不据此宣称实际瓶颈已确定。

选 VOICE-01 时保留完整 LLM 输出校验，验证一句已授权、已脱敏文本的 TTS 首块到达即可形成可播放前缀。定义供应方 adapter 的窄迭代接口，不建设供应方市场；明确音频格式、序号、结束/取消、背压和断线恢复。若需要调整二进制 framing、播放器或身份兼容，先补 09 §10/§16 和共享合同。

选 VOICE-02 时只做一个已登记 API 路径的增量文本，它不以 VOICE-01 已实施为前提，可在现有整句 TTS 边界验证收益。`live/dialog.ts:915–1026` 的项目锚定、屏幕投递与落账/结果话术闸，以及 `dialogLoop.ts` 的模型身份、工具参数和脱敏，必须在首句播出前保持有效。不能先说后校验，不能把未执行工具的结果当成事实。

若某条全局输出规则必须等完整回复才能判断，该轮继续缓冲；先确定允许早播的子路径及其证明，不为赶低延迟删除输出闸。跨 chunk 的 token/路径脱敏、半截 tool arguments、缺 model、取消后迟到 chunk、断线重连、未听到的句子不入 history，均是明确反例。打断时取消 provider 读取、TTS 队列与播放器，晚到音频不能复活。移动 native.reply 与浏览器音频是不同消费面，不把桌面一次通过外推全端。

验收仅覆盖所选路径：VOICE-01 在受控延迟 fixture 下首个可播放前缀早于整句合成完成；VOICE-02 在允许早播的文本路径首句早于完整回复。再做同配置、同输入、具名 artifact 的对照统计；只有真实环境证据支持才声明改善。单纯收到网络 chunk 不算实际出声。ASR partial、gist、第二供应方不在两批内。

开批先固定端到端改善目标及可接受的失败/取消回归界限，并约束并发请求、重试、缓冲和取消后的在途工作。流式不等于更便宜：连接与播放状态更复杂，打断后已生成的 token/音频可能仍计费；不增加供应方也不能承诺账单不变。维护责任落在所选 adapter/播放器路径；未达到收益目标或成本界限时停在旧的安全路径，不自动继续另一半改造。

### HOST-01/02：最小被动诊断可提前，同包验证与迁移按需

HOST-01（只读诊断 L2）复用 `status/health/readyz/setup`，报告安装版本、运行 identity、脱敏 state root、配置世代、pipeline 缺失或上游降级。默认不发 provider 请求、不执行第三方 CLI、不打印 key/完整路径。未获授权的 live 面只读取已提供的 fixture。判定能区分没起服务、旧 runtime、包不含 pipeline、配置未生效。

它可以早于分发改造成为独立小批：按用户请求聚合已有本地结果和已记录失败，未知就明示，不能为了填满诊断表自动调用模型或扫描外部账户。收益是减少排障步骤，成本主要是诊断映射随版本维护与一次按需本地读取；不新增后台探测服务或必填配置。不为它预先建设 upgrade/logs/uninstall 全套管理框架。

只有分发变更或可复现运行身份差异需要时，才在隔离 state root 上复用已有打包安装测试验证同一制品，不把它作为最小诊断的必做扩项；这不产生新 RC。HOST-02（L3）退出近期默认范围；具名分发需求成立后才选择常驻制品迁移或自动 upgrade，二者也不必须捆绑。前置 PG-05 恢复保障、daemon/pipeline identity 兼容、digest、原生依赖/许可、失败回滚。持续成本包括版本/兼容矩阵、恢复点与旧版本占盘；用户可能需要重启或短暂停用，迁移与退出方式须提前明确。部署、发布、重启常驻或迁移真实状态仍需当次 owner 授权。相同 bundle digest 只是其中一项验收。

### READ-01、A11Y-01 与其余候选

READ-01：退出近期默认范围；PG 安全链后，有一个现有工具不能满足的具名资料任务时，按 SP5/SP6 选一个只读来源，完成内容快照、出处、引用回读、截断/变化说明、导出验收；不默认 SaaS/OAuth/写入。持续成本是来源格式/权限变化、请求与快照保留，可能增加用户配置或授权负担；开批限定来源、容量和维护责任，无额外来源需求时不扩成连接器平台。工具输出视为数据，不能直接变 trusted 记忆。权限/外部输入边界按 L3。

A11Y-01：触达正式 TaskModal 时做键盘闭环（进入焦点、焦点限制、Escape、返回触发点），L2；几乎不增加远端请求或用户配置，维护随组件变化进行。后续快捷键另验证输入冲突与录音生命周期，不额外造全局系统。

CONTEXT-01：退出近期默认范围；只有固定长对话用例证明当前 pack/记忆/历史窗口丢失重要约束，且现有 pack/记忆修正不足以解决，才设计有预算、可回溯、可失效的 gist。持续模型调用、摘要保存与失真纠正都是成本；冻结调用频率/用量上限、失效规则和维护责任，不能证明约束保留改善就不启用。摘要不进入 trusted 记忆，不放在首响关键路径同步等模型；不以“廉价模型”替代可信度设计。

Pipecat 依赖、logger 背压、测试拆分和前端拆包：分别需要依赖闭包、故障/容量、耗时或加载证据。触达时处理，不先立治理批或大重构。额外语音供应方与本地模型同样后置：有现有供给无法满足的具名可用性、能力或成本需求，且维护者能承接凭据、协议、版本及本地资源成本时，只选择一种组合验证，不扩展成默认支持矩阵。

## 6. 执行纪律与下一会话

实施入口为 [IMPL-PROMPT](IMPL-PROMPT-engineering-gap-consolidated.astra.md)。默认取当前 PLAN-2 的具名 next 批；在本基线它是 PG-01B。未指定候选排产调整时，不重复向 owner 询问是否执行已授权的批内修复。

Grok CLI 承担实施；Codex/Astra 担任 supervisor，独立 Codex review 使用零上下文会话。当前 policy 的实施角色是 `grok-4.6/xhigh`，review 是 `gpt-5.6-sol/max`。新会话从 live policy 冻结，活动 cycle 用 frozen roles；不把“你来 review”解释成实施上下文里自评，也不擅自将 review 角色换成另一模型。

每个 candidate 一名 reviewer，最多 3 次产品修复和 3 次复审；P2 单账本、最终一次 sweep，完整门禁在语义 GREEN 后运行。复用既有测试，不为本文新增通用治理 checker。L3 先完成针对边界的独立方案审查；风险等级不改变模型与 reviewer 数量。

本方案不接受 8–11 天总工期承诺。先以首批的实际改动、review 和 gate 时长形成估算；实施输出应说明哪条操作/状态/观测能力变得正确、哪些界面可见，不能只汇报新增行数或脚本数。

## 7. 本轮验证边界

本轮保存了[比较探针](../../research/astra-gap-synthesis/comparative-probes.mts)，只调用生产纯解析器、内存 collector 并核对源码接线存在。它不证明真实 provider、实际运行日志、播放器分块或常驻部署可用。原看板/SQLite 探针仍有原报告说明的局限。新方案没有宣称这些建议已实施。

初版方案与实施 prompt 的独立审查及文件摘要见[初版收口记录](../../research/codex-findings/2026-09-05-engineering-gap-synthesis-astra-review.md)。本次成本取舍修订的独立审查、当前文件摘要与校验结果单列于[修订证据目录](../../research/astra-gap-synthesis/cost-revision/)；初版 GREEN 只对应旧摘要，不能替代修订审查。原 Fable/Astra 文件保持原样；本轮没有 commit、push 或安装。
