# 数据血缘摘要

应用事件先进入 `raw_events`，经过去重和内部租户过滤后写 `fct_product_events`。订阅事件来自支付平台和手工企业合同，两者在 `fct_subscriptions` 合并。

已知风险：

- 移动端 3.8.0 在 8 月 19–20 日重复发送 `project_created`；
- 企业合同取消日期由财务表格每周导入，最多延迟七天；
- 员工域名过滤表在 8 月 22 日新增两个测试域。

任何回填都要保留原始分区和变更批次 ID。
