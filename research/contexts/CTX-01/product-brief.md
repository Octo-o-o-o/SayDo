# 账单导出功能简报

目标：让工作区管理员下载单个自然月的账单明细，交给财务在 Excel 中核对。

已批准范围：

- 入口位于“设置 → 账单”；
- 输出 UTF-8 CSV，首行包含字段名；
- 字段固定为账单号、结算月份、项目、含税金额、币种、支付状态；
- 月份按账户时区计算；
- 仅管理员可导出；
- 50,000 行内须成功，超过时给出可理解错误；
- 验收包含 Excel 打开无乱码、金额合计一致、无权用户得到 403。

暂不包含：跨月合并、PDF 发票、自动邮件、会计系统直连。
