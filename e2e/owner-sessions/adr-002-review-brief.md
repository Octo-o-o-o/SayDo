# ADR-002 复核材料(observedModel 恒定族豁免)· 一页 · 2026-07-25 收口会话

> 性质:上浮 owner 的决策材料,**不代拍板**。收口对账时按交接指令整理;批复前豁免保持现状(休眠),不扩大。

## 一、背景与时间线(git 实证)

1. 09 §11 规则 2 原文:"BYOA 取流内 observedModel,缺失即作废"——运行时防 **family 谎报**的断言。
2. 实测(2026-07-24,ADR-002 §背景):`codex exec --json` **流内无 model 字段**;对 codex 严格执行 = codex_cli 永久不可用。
3. ADR-002(2026-07-24,提交 `ca102ea` 批次)按 provider 分档:**api/cursor_cli 严格**(缺失作废,有谎报面);**codex_cli/claude_cli 恒定族豁免**(族由二进制锁死,无谎报面;流内有 model 仍校验)。已回写 09 §11 规则 2(约 727 行)。
4. **owner 已裁定一次**(提交 `22d9290`,2026-07-24 17:33;ADR-002 文末段,段内标注日期 2026-07-25 与提交日期差一天,应为笔误):dev 期只用 cursor 一家两族(thinking=sonnet-4.5-thinking / evaluator=gpt-5),cursor 流内**有** model 字段,严格口径天然满足——**豁免不激活**;豁免逻辑(consume.ts familyFixed)保留,仅供未来 claude_cli/claude_sdk 接入时若流无 model 字段复用。配套防回归测试 dev-cursor.test 已落(本会话 CI 绿集合内)。

## 二、当前残留决策点(本次请批的唯一问题)

**canonical 09 §11 规则 2 中的恒定族豁免条款,保留还是删除?**

(运行时现状无争议:豁免休眠、cursor/api 严格口径全量执行、无任何调用走豁免分支。)

## 三、选项

- **A. 保留条款(现状,建议)**
  利:claude 订阅购入接入时(计划 v2.3① 顺延项)若 claude CLI 流内同样无 model,直接按已评审条款启用,免再走一轮 canonical 变更;安全论证成立(族由二进制锁死无谎报面;tripwire+笼 argv 独立防笼破;缺失豁免只作用于"无谎报面"档,fail-closed 方向不变)。
  弊:canonical 里存在一条"当前无人走"的分支,规则表面积略大。
- **B. 删除条款(一律严格)**
  利:规则最简,与"严格口径"字面完全一致。
  弊:claude 接入若流无 model 即全部作废、claude_cli 不可用,届时须再改 canonical + 再评审;删除并不提升现实安全(该分支现无流量)。

## 四、建议与理由

**建议 A(保留)**。理由:豁免的安全等价论证(ADR-002 §安全论证)经受了实施与收口两轮审视未被推翻;owner"严格口径不放宽"表态的语境是**有谎报面的 api 直连**,条款对 api/cursor 未放宽分毫;保留是为已知的未来接入(claude)预置已评审路径,删除只增加将来返工。

## 五、影响面

- 批 A:零代码变更,零行为变更;09 文本维持;claude 接入时按条款实测再启用(届时 evidence 留痕)。
- 批 B:改 09 §11 规则 2 + ADR-002 状态改 superseded + 删 consume.ts familyFixed 分支与 dev-cursor 相关断言(走轻量评审 + 回归测试)。

## 六、若无批复的缺省动作

维持现状(条款在、豁免休眠、不扩大适用面);claude 订阅购入触发接入时**再次上浮**,不自决启用。
