# 07 D9 分词 spike · 两批语料结果(2026-07-24)

复现:`node e2e/spikes/fts-d9/run.mjs [docsDir] [batch1|batch2]`(缺省本仓 `docs/` batch1;明细落 `spike-output*.json`)。

## 数据

- 第一批语料:本仓 `docs/`(01-11 + modules,17 文件,519 段落)入 FTS5 `tokenize='trigram'`
- 查询集:20 个领域术语(字面存在于文档),指标 hit@5(top5 段落任一属预期文档)
- **第一批历史基线 hit@5 = 20/20**(迁移前 462 段落)
- **单仓迁移后复跑 hit@5 = 19/20**(519 段落,2026-07-29;保持原验收 oracle)
- **第二批(2.3 奠基产物,SayDo 仓真实 bootstrap 的 `.saydo/knowledge/current` 四文档)hit@5 = 12/12**
- **2 字词专项 = 4/4 全部 0 命中**:"幂等"(存在于 11 个文件)/"记忆"/"审批"/"热词"——trigram 索引单元 3 字符,2 字中文词完全不可查(07 D9 已知限制实锤)
- 第二批发现:奠基产物是短行结构化文档,标题段常 <10 字符——切段阈值须按语料适配(batch2 用 5,batch1 用 30),否则标题词全丢(B5 生产侧入库以"claim 整条"为单位,无此问题)
- 单仓迁移后 batch2 只读 `current` 现役 generation；旧根路径由兼容 symlink 跟随 `current`，查询
  oracle 使用当前 canonical 字面“单仓协作约定”，不再借 generation 1 旧文本得到假阳性。

## 发现(第一轮跑分教训,已修正查询集)

口语措辞与文档字面漂移时纯子串匹配失败:如查"记忆分层"(文档写"记忆四层")、"预算截断"(文档写"超预算…截断")均 0 命中。trigram 无语义/同义能力,query 必须贴文档措辞。

## 单仓迁移复跑

迁入完整 canonical 后，第一批语料从 462 增至 519 段落。保持原 oracle 不变时，"产物库"的 top5 变为 `05/04/04/02/05`，未命中原预期的 `08` 或 `modules/b`，因此总分为 19/20(0.95)。这是语料扩充后的排序回归，不是路径失效;本轮不放宽 oracle，也不把它伪装成等价迁移通过，后续按 D9 召回质量债处理。

## 结论(P0 判定)

**trigram 起步可用**(2.2 生产配置维持 `tokenize='trigram'`):
1. 迁移后 >=3 字术语字面召回为 19/20;B5 是 FTS+rg 双源合流,代码事实永远 rg 现读,FTS 只答知识库;
2. topicTerms 来自 ASR 实际词 + 2.4 热词(M0 积累),会自然贴近文档措辞;
3. 两个限制的缓解已在既定路线:2 字词与同义措辞 -> 07 D9 降级链(simple 分词扩展 P1 / 预分词 / 最坏 rg-only+结构图),不阻塞 P0。

两批语料均已跑(第二批 2026-07-24 随 2.3 补测);中文召回若在 dogfood 中不达标,按 07 D9 风险表走降级链。
