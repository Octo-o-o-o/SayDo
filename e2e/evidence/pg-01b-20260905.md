# PG-01B 证据:远程业务面止损与现役口径收敛

本文件记录 PG-01B 的代码提交 `ebd449080bb0e476eb2dd3334ee0b152cb3a7eeb` 的验收证据。
本文件随其后的 `chore(evidence)` 提交入库,不自指。

## 候选身份

- I=`ebd449080bb0e476eb2dd3334ee0b152cb3a7eeb`
- tree=`20e3303197f602d27548a4ab7d4490263fb85272`
- parent P=`bcf8ea855f25b177888d9159f75e49214f3dd892`
- `git rev-list --count P..I` = 1(单一 implementation commit)
- P..I pathset 共 44 项;工作树与 index 全程 clean

主仓 `main` 已由 `git fetch <clone> <branch>:main` 快进到该提交(纯引用更新,未 checkout,
未触碰当时正在另一分支作业的主工作树)。快进后 `main^{tree}` 与候选 tree 逐字节一致。

## 语义评审

owner-recovery-3 内两轮**互不知情的零上下文 reviewer**,均为 GREEN:

| ordinal | 报告 SHA-256 | bytes | verdict |
|---|---|---|---|
| 1 | `2f9d8831005bed46d4df6fde268607db422eb3e5346b247903aa7cfaac52eacb` | 37034 | GREEN |
| 2 | `571802de75e51b7c9bd9d4c28d9d8aaa87f35518513227285e7ddb761b1ad4c0` | 35512 | GREEN |

ordinal 1 经 `validate_review_manifest.py` 判 `valid` / `next_action=full_gate`。
ordinal 2 的 manifest 结构经独立 `json.loads` 校验通过,但其 receipt 未能入账(见「记账缺口」)。

归档:评审输入 `prompts/2026-09-05-pg-01b-owner-recovery-review.md`;
报告 `research/codex-findings/2026-09-05-pg-01b-owner-recovery-review.md`。

## 冻结合同的七项必跑门禁

全部经 `cycle_control.py --record-gate` 由工具执行并记录(收据带 `producer_sha256`、
`entry_sha256`、`gate_spec_sha256`),candidate 寻址到本 I:

| 门禁 | exit | 结果 |
|---|---|---|
| focused-console | 0 | 34 files / 283 tests |
| focused-daemon | 0 | 131 passed \| 2 skipped;2184 passed \| 6 skipped |
| pairing-corpus | 0 | 216 passed |
| remote-inventory-check | 0 | exact-set 与四个 composition root 双向对账 |
| remote-inventory-mutations | 0 | self-test 8 passed |
| just-ci | 0 | node + python 双矩阵 |
| playwright | 0 | 38 passed |

`just ci` 含 `check-public-tree-privacy.mjs --ref <40 位 SHA>`(本 clone 已装私有探针锚):
`scanned=1991 binary=108 excluded=8 hits=0`;python 侧 ruff 全过、pytest 34 passed。

Playwright 会改写 `e2e/screenshots/**` 下受管截图。本批把「跑前清理 47188/47189/47120 残留监听
与 `/private/tmp/saydo-playwright-home-*`、跑后 `git checkout -- e2e/screenshots` 并保留 exit code」
固化进受哈希锁定的门禁入口脚本,不再依赖执行者每次复述。所有门禁跑完 `git status --short` 为空。

## 本批做了什么

1. **abandon lifecycle 断言补齐**:`focus_events` 的 `lifecycle_changed` payload 中 trim 后 reason
   此前无断言,16/16 对该验收条件假绿。
2. **远程 WS fail-closed 的配套收敛**:`voice/hub.ts` 新增守卫使既有契约测试
   `focus-v04e-screen-a7-a6.test.ts` 确定性失败;该用例已改写为断言 tailnet console WS 被 4003 拒连,
   `docs/09` §15.1 同步。
3. **canonical 现势口径清扫**:`docs/02`、`docs/04`、`docs/06`、`docs/09`、`docs/11` 中仍以现在时
   描述 direct_to_review 与远程业务面为现役的表述,统一标注 `designed/deferred`
   (远程相关用既有 `DF-REMOTE-REOPEN`),保留历史原文不删。修正了 `docs/10:73`
   「02 已回改对齐」这条当时为假的交叉引用。
4. **测试面收敛**:`e2e/console/console.spec.ts` 6 条 M1 移动 LAN 用例断言的是 PG-01B 已关闭的
   远程业务行为,导致 playwright 稳定 RED。全部 1:1 改写为断言 fail-closed 契约
   (403 `remote_business_forbidden`、WS close 4003、业务树 count 0),并保留
   `/health` 200 与静态壳 200 的正向断言以防「标过头」。用例计数守恒,无删除、无 skip。
   `t2-thin.test.ts` 与 `SetupBootstrapBoundary.test.ts` 的同类断言一并翻转。
5. **owner 特批的三条覆盖回退补回**(`p2_immediate_fix_requires_owner`,owner 于 2026-09-05 批准):
   - `sendTtsSay` 的 `redactText` 出口接线新增两条集成断言(纯文本路径与 `redactSpans` 路径)。
     此前唯一样本为「本地口播」,不含可脱敏内容,删掉产品侧接线测试仍会全绿——该处是
     AGENTS.md 硬规则 4(TTS 脱敏)的守护面。
   - `queueText` 成功路径:全仓此前无任何用例点击 `.m-send`。
   - `queueText` catch 分支(发送失败保留草稿)此前无浏览器覆盖。
   三条均以本机 `via="local"` 路径覆盖,不依赖已关闭的远程面。

## 脱敏断言的负向验证

supervisor 在候选上做变异对照,证明新断言不是又一层假绿:

```text
基线   pnpm --filter @saydo/daemon exec vitest run test/voice-hub.test.ts -> 51 passed, exit 0
变异   把 voice/hub.ts 的 redactText(msg.text, redactSpans) 换成 msg.text
       -> 2 failed | 49 passed,恰为新增的两条脱敏用例
还原   git checkout -- packages/daemon/src/voice/hub.ts -> 51 passed, exit 0
       工作树 clean、HEAD 未变、candidate_fingerprint --expect exit 0
```

两名 reviewer 各自独立以代码路径论证同一结论(未做变异实验):路径样本命中 `redactor.ts` 的
`PATH_RE` 且 label 判 `.env` 成立 → 「某个配置文件」;span 样本走显式段替换 → 「一处客户数据」;
两者均在 `redactor.test.ts` 有对应单测。第二名 reviewer 另追至 `useVoiceChannel.ts` 的唯一发送点,
确认 `sendText` 无内部 try/catch,故草稿保留用例的猴补确实进入 catch 分支。

## 记账缺口(如实记录,未粉饰)

本批的 supervisor 记账有四处失误,均由机器校验器挡下,未污染产品结论:

1. 四份复审 handoff 缺 `workflow-v2` / `roles` 契约块,且未按规则在派发前运行
   `validate_handoff.py`。补齐后该校验器返回 `valid`。
2. owner 特批的 P2 修复被放进已取得 GREEN 的 cycle `pg01b-owner-recovery-2`,
   `--advance` 正确拒绝(`green_without_completed_rereview` + `unauthorized_candidate_change`),
   `--release` 亦拒(`reservation_started`)。该 cycle 账本停在一个无法结清的 started repair 预留。
3. cycle `pg01b-owner-recovery-3` 中为 ordinal 1 做了不必要的预留;当时该 cycle
   `applied_receipts` 与 `cost_events` 均为空,重置未抹除任何真实记录。
4. 同 cycle 为 ordinal 2 做的预留无法结清:校验器要求 `ordinal >= 2` 时
   `used_repair >= ordinal - 1`,而本 cycle 的 repair 发生在前一 cycle。该悬挂预留使
   `cycle_control.py --finalize` 返回 `hanging_reservation`。

因此**本批没有 `--finalize` 记录**。owner 已知情并决定按现有证据合并,finalize 与上述
handoff 模板合规作为流程基建跟进项另做。所有失败的 receipt 与状态快照保留在
`logs/pg01b-owner-recovery-*/`,未伪造收据、未手工改写状态以抹平。

另需记录:`~/.octoworkflow/` 全套脚本在本批作业过程中被另一进程整体升级(时间戳 2026-09-05
13:28)。可判定证据:同一份 ordinal 4 报告,升级前 `validate_review_manifest.py` 返回
`status=valid`,升级后同样命令返回 `missing_control_dir`。本批已按新契约重建控制目录
(schema 2 冻结、hash-locked gate spec、receipt 绑定字段、coverage matrix 经
`--check-coverage-matrix` 判 `coverage_matrix_ok`)。

## Deferred P2

本批复审共登记若干 P2,全部按 `p2_default_action=record_and_defer_to_final_sweep` 保持 open,
不在本批修复。其中值得优先关注的一条:`candidate_fingerprint.py` 只哈希工作树相对 HEAD 的脏
diff,工作树干净时其值为常量、不绑定候选内容。仓内可证:`logs/pg01b-cycle-20260904/coverage.json`
对 `candidate_head=1cd08b47...`(tree `4ddd90c7...`)记录的 fingerprint 与本候选(tree
`20e33031...`)完全相同。候选身份实际由 head/tree/parent/rev-list 建立,fingerprint 仅用于
证明工作树未被污染。

## 边界

本批是本地闭环:语义 GREEN、七项冻结门禁本地全绿。这不是托管 CI、不是真人场次、不是部署验收。
未 push、未发布公开快照。owner decisions D3/D6 仍未签,未被当作已放开;Gate 0 无 bypass、
S0-S3、审计不可变、TTS 脱敏均未放宽。
