# P0.5 · 契约封闭 + Hopper 桥 + 直达验收档 + Demo — 验收证据(2026-07-25)

## 1. 测试命令与尾行输出

- `just ci`:contracts 65 + daemon 全绿(P0.5 各批提交时 303 -> 316 -> 326 -> 收口 335)。
- `pnpm exec vitest run test/p05b-fake-runner.e2e.test.ts`(对锁定二进制,零 LLM 成本):**3 passed**。

## 2. §12 条目 ↔ 测试对照

| §12 条目 | 测试 |
|---|---|
| §12-2 全反例(⑥⑦⑧ grant 实例类) | p05c-direct-mode.test(ttl 执行点复验/lifecycle scripts miss/保护分支既有) |
| §12-6 取消/改需求(跨域) | p05a-contracts.test(CancelProof=RunSettled)+ p05b(unblock 幂等链) |
| §12-7 跨域子集(binding NULL 重放/journal !=confirmed 重放) | p05b-bridge.test(pendingDispatches/unconfirmedCommands) |
| §12-8 Hopper 消费全套 | p05b-bridge.test(cursor/半行/损坏/未知/收缩/RunSettled 复核按终态拆分)+ p05b-fake-runner e2e |
| risk 双维反例(X3) | p05a(ready∧high 禁自动)+ p05b(retryGate high 转人工/分诊 blocked 走 re-drop) |
| capabilities 断言正反例 | p05b-bridge.test(锁定点漂移/缺 RunSettled/缺枚举/不可解析全拒) |

## 3. fake-runner e2e 实录(锁定副本 bdd1e548,§12-8 harness)

1. **completed 全链**:握手断言 -> C1 渲染产物直接投喂 -> lint 三处判定放行 -> drop(stdin)-> scan ->
   HOPPER_FAKE_SPEC run -> finalStatus=review -> C3 断点读(0 corrupt)-> RunSettled 廉价复核
   (evidence_digest 与 review show 一致 + summary 存在)-> **trust-report .md 受控映射 .html +
   呈现层转换零 emoji(越界/扩展名反例拒)** -> **review approve --req-id 幂等重放同结果 +
   --receipt/--origin 审计透传 -> review show approved=true**。
2. **blocked 链**:RunSettled{blocked} 事件即证据(summary 空)-> **unblock --req-id 幂等 ->
   scan 后回到可调度档**(0.5 八条之"blocked 应答回注"承载)。
3. **hopper check = Tier1 确定性验收 oracle 可行性(0.5 顺做项,09 §9)**:sandbox repo staged diff +
   acceptance[] 经 stdin --criteria - -> schema_version=1 + changed_files 正确 + verification=passed
   (真跑 npm test)+ --no-llm 全闸门照跑 + 不写事件流——"生成方不自评"的机械承载成立。

## 4. 0.5 窄闭环 PoC 八条对照(05 §3;自动化形态承载,替代人肉手动版——形态偏离如实标注)

> 计划 0.5 原型是"人肉遥控一单"。实际以 fake-runner e2e 自动化承载(可重复、进 CI、零成本),
> 强于一次性手动;逐条对照如下。真人现场版归 P0.5-D 场次④(owner)。

| # | 红线/验收 | 承载证据 |
|---|---|---|
| ① 语义="执行和闸门已结束等验收",不说完成 | statusMapping(review+settleOk=ready_for_review;golden 状态词断言) |
| ② 语音"好"不直转 approve+merge | 封闭肯定词表 + S3 人工合并交接(SayDo 不自动调 hopper merge) |
| ③ 回叫等 settle barrier | consumeRunSettled 复核(evidence+summary;callback settle 缺一不叫) |
| ④ 事件重放幂等/gap/损坏行 | events.ts 断点读 e2e(半行/损坏/收缩)+ §12-6/7 测试 |
| ⑤ bridge 重启只回叫一次 | outbox dedupe 活跃唯一 + journal !=confirmed 重放(p05b-bridge) |
| ⑥ verify 失败报"闸门阻断"非"完成" | fake-runner 场景矩阵(VerificationFinished{failed}=>failed 投影;statusMapping) |
| ⑦ 禁外部副作用(独立测试仓) | e2e 全程 mktemp vault+repo;fake-runner attemptPush 由 Hopper wrapper 拦 |
| ⑧ 全链审计可追 | dispatch 注释行幂等锚 + x_saydo 溯源 + --origin/--receipt 透传 + G5 意图链 |

`hopper lint`/`hopper check` JSON 证据各一份:e2e 运行时产出(lint 放行断言/check schema 断言)。

## 4b. 追加实证(2026-07-25 上午,对锁定副本手工探针;为回答"要不要再问 Hopper"先自答)

- `review show` 规范字段 = **`evidenceDigest`**(camelCase;`evidence_digest` 为 null)——SayDo 双兼容读法保留;
  **`prospectiveTreeSha` 就在 review show 里**(路径二 approve 落库树基准的直接来源)。
- **B4 判别键实证**:真实制造 rebase 冲突 => `MergeFinished{status:"conflict"}` 事件 + 投影 `conflict`;
  triage 来源 conflict 无 Merge 事件——C3 按事件流判 conflictSource,无需 Hopper 侧改动。
- **merge 门语义**:未 approve / approval 无 binding / acceptance=needs_human 未 waive => `blocked_gates`
  退出 1(JSON 列 blockers),**不发 Merge 事件、投影留 review**——门拒 != 冲突;
  且 **acceptance=needs_human 时 approve 需 `--waive`**(否则 approve 静默不绑定)——P0.5-D 现场
  与 blocked 回叫话术需知此分支。
- approve 输出 = MutationResult 形态 `{req_id, schema_version, status:"applied", event_ids, reason}`(A3 契约吻合)。
- `last_run_cost`:fake-runner 恒 null(附录 §2.5 已注)。**Hopper 回执后闭环(appendix §3.1,2026-07-25)**:
  known:true 真实形状 = `{known, value, currency:"USD", as_of}`(字段是 value 非 amount、币种 USD)——
  extractLastRunCost 已改真实形状(旧 amount/缺币种一律 unknown 不猜);全链分币种(cost_entries 落 USD
  如实不折算,console/valueReport 合计按币种分列,混币种合计=编数)。
- **RunSettled{recovery:true} 端到端已验证(appendix §3.2 触发法,2026-07-25 对锁定副本实跑 + 固化
  p05b-recovery.e2e.test,SAYDO_SLOW_E2E=1 门控约 80s)**:kill -9 -> 等 65s lease -> cancel =>
  `RunSettled{recovery:true, runner_status:cancelled, final_status:failed}`,consumeRunSettled 按
  "事件即证据"路径 settle 通过;两坑(expired 请求保留会被补 applied / reconcile 先跑走
  RecoveryRecorded 不发 RunSettled)已录入测试注释。

## 5. 偏离与回写

- 0.5 未按原计划在 Phase 0 期间执行(当时 Hopper 前置未落地),改造为自动化形态随 P0.5-B 落——
  能力覆盖超集,唯"owner 抽查"留场次④。
- P0.5-A reviewTask reject 承载于取消链(§13 返回词勘误挂下轮 canonical 回写)。
- baseline.2 切锁(X1 条件达成):~/.saydo/config.toml expected_version -> bdd1e548。

## 6. 代码提交

`cafde9d`(A) `56d8b89`(B) `922f6e6`(C/E) + 迟到评审回修 `e50668e` + 收口批(本轮:0.5 改造/
价值证据轨/trust-report 渲染)。
