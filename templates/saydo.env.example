# SayDo 密钥模板 —— 本模板可入 Git;复制为 ~/.saydo/.env 后填真值，真值文件永不进 Git
# daemon 的模型配置用 env:VAR 引用这里；P0 语音 pipeline 直接读取下列 ASR/TTS 环境变量。

# ---- 大模型 ----
# 已接线且自检通过的 CLI 可以走零 API key 慢速文本(时延不作固定秒数承诺,登录不等于免费);要 API 对话或全 API 再填下列 key。登录不等于订阅权益。
OPENROUTER_API_KEY=         # 仅当对应槽走 API/via=openrouter 时需要;openrouter.ai/keys 生成,一 key 通多家
DEEPSEEK_API_KEY=           # 最省事的单家上手路径:只填这一个,四槽经 [providers.api.deepseek](09 §11 样例);四槽同族 ⇒ 向导会前置勾选同族确认(11 §5)
OPENAI_API_KEY=             # 可选:直连 OpenAI 官方(billing 正常时);dev 缺省不需要
ANTHROPIC_API_KEY=          # 可选:仅某槽位改用 Claude 系 API 直连时需要

# ---- 语音 ASR/TTS ----
DOUBAO_TTS_API_KEY=         # 【重要】火山豆包 seed-tts-2.0 大模型 TTS(07 D5;v3 双向流式,X-Api-Key header 鉴权)。同 repo-demo-recorder 用的 key
VOLC_APP_ID=                # 【重要】火山豆包流式 ASR(工程 ADR-101 定档),与 access token 成对提供
VOLC_ACCESS_TOKEN=          # 【重要】火山豆包流式 ASR access token;不是 TTS 的 X-Api-Key

# ---- 三方对比端点 key(按 config [providers.api.*] 实际建的档填;全部可选)----
# OPENROUTER_API_KEY / DEEPSEEK_API_KEY 见上。以下为其余单家直连,想单独对比时再填:
# MOONSHOT_API_KEY=         # 月之暗面 Kimi
# DASHSCOPE_API_KEY=        # 阿里百炼(通义千问)
# GEMINI_API_KEY=           # 直连 Google(经 OpenRouter 则不需要)

# ---- 执行 agent 凭据(开发档 = Tier 1 执行)----
# Claude Code:用上面的 ANTHROPIC_API_KEY 或 Claude Code 登录态(二选一,确认额度)
# Codex/Cursor 的本机登录态也可承担推理槽(见配置模板 CLI 绑定);开发执行链另需 [tier1] 两键。

# ---- 回叫推送 ----
# 不在向导白名单,需手改
NTFY_TOPIC=                 # 随机串(如 saydo-<你的随机字符>);手机 ntfy app 订阅同名主题
NTFY_SERVER=https://ntfy.sh # 或自托管地址

# ---- 评审(实施期用)----
# Codex CLI:确认 `codex` 可用(轻量评审"攒批 Codex"依赖它);不可用则降级为 subagent
