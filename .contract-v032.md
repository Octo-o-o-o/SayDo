# SayDo Focus Contract v0.3.6 增量方案——交互结构与新对象

> 2026-08-05 凌晨。v0.3->v0.3.1(一审)->v0.3.2(二审)->**v0.3.3(吸收[三审](../review/2026-08-05-v0.3.2方案-codex三审.md)放行门 8 条+全部 partial 尾巴)**。叠加于 [v0.2.1](2026-08-04-SayDo-Focus-Contract-v0-方案.md)(amend:§4.1 lifecycle、§9 迁移纪律)。
> 三审已确认 fixed 的骨架(不再改动):确认环 DB-authoritative(§5.1)/needs 非全函数修复(§5.3)/lanes 复合 FK+parent(§3.1)/lifecycle revision(A-N8)/v21 rebuild 认错重做(§9)/title 占位(§2.1)。本版关键修正:**archived 停机矩阵(§4.1)/blocked 球权按 status 不动 needs(§3.4/§5.2)/binding 条件照实(§5.2)/redo 只认 exact set(§3.3)/确认消费与 mutation 同事务(§6.1)/lane 树无环+父线 retire 前置(§3.5)/预期产物=role 化 artifacts(§4.2)/迁移版本表统一(§9.3)**。
> v0.3.4(定点):吸收[四审](../review/2026-08-05-v0.3.3方案-codex四审.md) 4A/4B/3C——binding/redo exact set/确认原子性三项 fixed 不重开;A-1 waiting 列裁定/A-2 批 3 零主动通知/A-3 activation lifecycle 门/A-4 parent 未 retired 断言/B 四项/C 三项全 closed。批 1/1.5 已按合同施工落库(4222c6a/ce0e381)。状态:五审判 4A=3fixed/1partial+新 A(换锚原子性)——v0.3.5 全闭([五审](../review/2026-08-05-v0.3.4方案-codex五审.md)):A-2 矛盾行改写/换锚三路径先验后关并入同 WriteTx/B-2 payload 冻结字段全列/B-3 realized 快照/C-3 内联去历史依赖。六审判 3fixed/2partial(纯落文级)——v0.3.6 补齐([六审](../review/2026-08-05-v0.3.5方案-codex六审.md)):换锚负例入 §10/§9.1 rebuild 程序内联。**批 3 域 A 清零,放行施工**;批 2 已落库 e56bd27。

## 0. 吸收台账

三审放行门:(1)lifecycle 转换+停机矩阵->§4.1;(2)dependency waiting_on 兼容+blocked 球权->§3.4/§5.2;(3)binding 条件->§5.2;(4)redo exact set->§3.3;(5)确认 CAS 同事务->§6.1;(6)lane 树无环+父线 retire->§3.5;(7)artifact/预期产物+rebuild 版本->§4.2/§9.3;(8)renderer/事件域 matrix+版本->§10/§9.3。partial 尾巴:A1->§4.1;A2->§2(权威口径:至多归一个空间,交互定稿措辞随勘误);A3->§1(session_project_events 裁定);A6->§6.1;A9->§3.3;A11->§4.2;A13->§5.2;A15->§10;B5->§1(最小样本);B-N1->§5.4;B-N2->§5.2;C2/C-N1->§5.2 索引;C3->P-b 移 §12;C-新2 路由笔误随全文修正。

## 1. 会话主轴倒置(三窗)

三步 A/B/C 与三窗(壳项目清单->sessions rebuild->authority cutover,窗间>=3 天观察)沿 v0.3.2 定稿(git pin a167f4c);本版补三点:
- **session_project_events 裁定(A3 尾)**:该表 project_id 保持 NOT NULL——无 project 会话**不产生** project 事件(表语义=项目归属变更史,无归属即无史);detach(会话从 project 解绑)本批不提供路径(未出现该需求),写入待设计。
- **判负门统计合同(B5 尾)**:观察期最小样本=无 project 会话>=30 场,不足则观察期顺延一周(最多两次,再不足=功能未被使用,判负回炉重估需求);分母全部写死(失败率=失败调用/该类调用总数)。
- 假设清单产出物同 v0.3.2。

## 2. 空间模型

DDL 与删除事务沿 v0.3.2 定稿(pin a167f4c;focus_spaces 表+focuses.space_id+两索引已随批 1 施工落库 4222c6a,以库内实现为准)。**单归属权威口径(A2 尾)**:一个 Focus **至多归一个**空间(space_id 可 NULL=未分组);交互定稿 §1"每个 Focus 归一个空间"随实施计划勘误为"至多一个"。空间删除的确认漂移防护见 §6.1。

## 3. 线(lane)模型

### 3.1 DDL(批 3;v23 obligations rebuild 同窗,§9.3;C-3 内联定稿)

```sql
CREATE TABLE focus_lanes(
  focus_id TEXT NOT NULL REFERENCES focuses(id),
  id TEXT NOT NULL,                          -- lan_ 前缀 ULID,daemon 生成;用户只改 title
  title TEXT NOT NULL,
  parent_lane_id TEXT NULL,                  -- 树:从哪条线分出(NULL=从主线)
  created_from_event INTEGER NOT NULL,
  retired_at TEXT NULL,
  PRIMARY KEY (focus_id, id),
  FOREIGN KEY (focus_id, parent_lane_id) REFERENCES focus_lanes(focus_id, id),
  FOREIGN KEY (focus_id, created_from_event) REFERENCES focus_events(focus_id, seq)
);
-- v23 rebuild 给 focus_obligations 加:lane_id TEXT NULL(复合 FK (focus_id,lane_id))
--   + created_from_event INTEGER NULL + waiting_on_obligation_id TEXT NULL(§3.4)
CREATE INDEX idx_obligations_lane ON focus_obligations(focus_id, lane_id);
CREATE INDEX idx_obligations_waiting ON focus_obligations(waiting_on_obligation_id)
  WHERE waiting_on_obligation_id IS NOT NULL;
```

### 3.2 拆分(内联定稿)

`proposeLaneSplit`(倒计时确认)payload=`{focusId, lanes:[{title, parentLaneId|null, obligationIds:[...]}], baseline:{focusRevision,eventHWM,obligationsDigest}}`;消费事务=建 lanes 行+`lane_split` 事件+逐义务 `UPDATE ... SET lane_id=? WHERE id=? AND focus_id=?`(双断言)。

### 3.3 从某步重走(A-新4 重设计:执行只认 exact set,推导降级为建议)

**放弃"created_from_event>anchor"作为执行依据**(它无法表达 lane assignment 史:早建后移入会漏、他线建后移入会误伤)。合同:
- redo 提议(UI 或语音)呈现该 lane **当前全部未结义务清单**,每项带勾选;默认勾选状态用 `created_from_event > anchorSeq` 仅作**建议**(UI 标注"按创建时间预选,请核对");
- 执行 payload=`{laneId, anchorSeq, supersededIds:[...]}`(**exact set,用户所见即所勾**)+baseline 三元组;消费=逐 id 断言(属该 focus+lane+未结)+批量 resolve(superseded)+`redo_from` 事件(记 anchor+exact set)。
- `unknown_origin` 概念删除。**created_from_event 迁移细节(B-1)**:v23 加列 INTEGER NULL;回填=从 obligation_opened 事件按 dedupeKey 反查 seq,匹配不到留 NULL(NULL 行为:redo 建议不预选、航迹归主线起点段);新写入=同 writeTx 内先插事件取 seq、行随之写值。

### 3.4 依赖与唤醒(四审 A-1/A-2 定稿)

- **列裁定(A-1)**:实现现有 `waiting_on TEXT`(自由文本描述)+CHECK `waiting/blocked=>waiting_on NOT NULL`——**保留不动**;v23 **新增** `waiting_on_obligation_id TEXT NULL`(结构化依赖,引用同 focus 义务 id)。设置=同语句写三值:`SET status='waiting', waiting_on=:preTitle(人读文本,满足既有 CHECK), waiting_on_obligation_id=:preId`;清除同清两列。validator(写入口):id 列非空=>statusin(waiting,blocked)AND前置同 focusAND无环(上溯<=16);无存量回填(新功能)。索引 idx_obligations_waiting 建在 id 列。
- 唤醒矩阵:done->open+清两列+woken 事件;abandoned/no_longer_applicable/superseded->**blocked(两列保留作 provenance)+blocked 事件**——不改 needs;blocked 球权按 §5.2 status 单列。
- **通知裁定(A-2)**:实现仓唯一 durable outbox(callback_outbox)要求 task_id,义务依赖无承载——**批 3 零主动通知**:wake 只写账本,用户经看板 attention 自然看到;主动通知整体移待设计(§12)。
- UPDATE 恒 `WHERE status='waiting' AND waiting_on_obligation_id=:pre`;幂等同前。

### 3.5 树不变量(A-新6)

- 建 lane(含 split 消费)写入口断言:parent **存在AND同 focusAND`retired_at IS NULL`(A-4)**+`parent_lane_id != id`+沿 parent 链上溯无环(<=16,超出拒 `lane_depth_exceeded`);
- **父线 retire 前置条件:无未 retire 的子线**(有则拒 `lane_has_children`,话术引导先处理子线)——不做级联 retire(破坏面不可见);
- matrix 补正反例:自指拒/成环拒/带子线 retire 拒。

## 4. archived 态与产物

### 4.1 lifecycle 转换与停机矩阵(A-新1/A1 定稿;amend v0.2.1)

转换表(唯一权威;未列出的转换一律拒):

| from->to | 触发 | 事件(升 revision) |
|---|---|---|
| captured->active | 首次实质推进(既有) | lifecycle_changed |
| active<->dormant | 等外部/外部回信(既有) | 同上 |
| active->archived | 用户归档(理由必填) | 同上 |
| archived->active | reopen | 同上 |
| active->closed | 收尾结算(既有 close 链) | 同上 |
| active/archived->abandoned | 用户明确放弃 | 同上 |
| closed/abandoned->* | **无出边**(closed 唯一后续=fork 新 Focus) | — |

**停机矩阵**(非 active/captured/dormant 的 Focus,系统"不盯"的机械定义):

| 面 | archived/closed/abandoned 行为 |
|---|---|
| attention(§5.2) | 全部 union 分支 `JOIN focuses f ON ... AND f.lifecycle IN ('active','captured','dormant')`;pending_confirmations 行按 focus_id 同滤(focus_id NULL 的确认不滤) |
| 依赖唤醒(§3.4) | **账本照写**(woken/blocked 数据落地,跨归档一致性);**批 3 不写任何 notification outbox**(零主动通知,五审 A-2 定稿);未来主动通知面设计时必须带 lifecycle send-time fence;reopen 后 attention 自然重现,零恢复逻辑 |
| 通知/回叫 | (主动通知面待设计,本批无;仅指既有任务回叫链,按 lifecycle 滤) |
| 看板 | archived=极轻行(交互定稿);closed=产物卡 |
| archive 事务 | 关闭该 focus 全部 active activation;运行中任务**不叫停**(settle 照落账);**不清 sessions.primary_focus_id**(留锚,经 reopen 话术引导——比隐式清锚可解释) |
| **activation 硬门(A-3)** | `startActivation` 断言 lifecycle IN ('active','captured','dormant') 否则抛 `focus_not_active`;**`ensureActiveActivationForSession` 自愈同门**(不自愈,错误带 reopen 提示话术"这个 Focus 归档了,要先重新打开吗?");captured/dormant 自动提升保留;reopen=显式 lifecycle 事务先行;**换锚原子性(五审新 A)**:switchAnchorActivation/确认换锚消费/undo 恢复三路径统一为**先验目标 focus lifecycle 通过才关旧 activation**——实施合同=关旧+开新+换 anchor 并入**同一 FocusWriteTx**(目标不合法=事务不开始,零副作用拒;undo 恢复目标已 archived/terminal 时=不动当前 activation,如实播报"原来的 Focus 已归档,撤不回去了") |
| 可接续清单(B-4) | contextSection [可接续的 Focus] 排除 archived(NOT IN closed/abandoned/archived)——reopen 入口在看板归档区 |
| 新写入 | archived focus 上的 propose/记事=拒(`focus_not_active`,话术"这个 Focus 归档了,要先重新打开吗?"——确认后 reopen+重试) |

### 4.2 产物模型定稿(A11 尾/B1/(7))

```sql
CREATE TABLE focus_artifacts(
  id TEXT PRIMARY KEY,
  focus_id TEXT NOT NULL REFERENCES focuses(id),
  kind TEXT NOT NULL,                 -- zod: file|git_commit|url|text
  role TEXT NOT NULL,                 -- zod: expected|deliverable|input|reference
  title TEXT NOT NULL,
  ref_json TEXT NOT NULL,             -- zod:{repo?,path?,commit?,url?,digest?,note?} 至少一键;expected 可 {}
  copied_from_artifact_id TEXT NULL,  -- fork lineage
  created_from_event INTEGER NULL,    -- FK 债:v24 rebuild 补复合 FK(§9.3);此前写入口断言
  created_at TEXT NOT NULL
);
```

- **「预期产物」= role='expected' 行**(ref_json 允空);**realization(B-3)**:expected->deliverable=`UPDATE ... SET role='deliverable', ref_json=:final WHERE id=:id AND role='expected'`(changes==1 CAS)+`artifact_realized` 事件(payload 存 ref_json 终值=历史快照);收官卡 chips=deliverable,头卡=expectedUNIONdeliverable;
- fork 复制:deliverable/reference 行复制为新 Focus 的 input(带 copied_from);
- version/invalidation:ref_json.digest 承载,深化留待设计。

## 5. attention read model

### 5.1 pending_confirmations(三审 fixed,不动)

### 5.2 attention SQL(A-新3/(2)(3)/B-N2 尾/索引定稿)

- **active binding 条件照实**:`ab.phase IN ('authorized','bound') AND ab.superseded_by_binding_id IS NULL`(ddl 真实枚举;A-新3 改正)。
- lifecycle 过滤:全部分支按 §4.1 停机矩阵。
- orange 增补分支:`focus_obligations WHERE status='blocked'`(**任意 owner**——前置已终止需人裁决;title 展示前缀"前置已终止:";与 needs 无关,§3.4 呼应)。
- **task 与 mapped obligation 去重(B-N2 尾)**:实现仓已有 taskObligationMap——task 行进入 attention 时,`JOIN` 该映射把对应 obligation 排除(task 行让 obligation 行退位,task 信息更实时);pending confirmation 引用去重用 `json_each(payload_json)` 精确取 id,不做字符串 LIKE。
- 索引(C-N1 定稿):`CREATE INDEX idx_obligations_attention ON focus_obligations(owner, status, needs, updated_at)`(全局看板首列不含 focus_id);既有 (focus_id,owner,status) 保留服务详情页。
- 其余(OPEN_SET/id 命名空间/排序/unknown 橙尾)同前。
- lane id 格式(C-1):`lan_` 前缀 ULID(newId 惯例)。
- pending_confirmations.focus_id 规则(C-2):kind 以 focus_ 开头且 payload 含 focusId=>列必填;仅 focus_create_anchor 允许 NULL——NULL 行不受 lifecycle 过滤(新建确认恒展示)。

### 5.3 needs(三审 fixed,不动)

### 5.4 session_task_context(B-N1 尾定稿)

`session_task_context(session_id TEXT PRIMARY KEY REFERENCES sessions(id), ref_kind TEXT NOT NULL, ref_id TEXT NOT NULL, nonce TEXT NOT NULL, set_at TEXT NOT NULL)`——set 时校验 target 存在(obligation/task 按 ref_kind 查行,无则拒);**TTL=2 小时**(读取时惰性判过期即删);**daemon 重启清全表**(UI 瞬态,重启即失效,audit 记数);弹窗关闭 DELETE 带 nonce(开新弹窗换 nonce,旧关闭请求 CAS 不中);会话换锚/收尾时同步 DELETE。pack 注入段:行有效时注入"[正在处理] {target 摘要}"。

## 6. 确认 digest 与消费事务(A-新5/A6 定稿)

### 6.1 同事务合同

- **不变量:pending 行删除与语义 mutation 原子**。实现合同:consume 入口(词表/click/auto-accept)先做**内存预占**(session 内标记 receipt 消费中,阻断并发通道)-> baseline 重校+现势复核(不过=释放预占+stale 终局)-> **进入消费端 FocusWriteTx:事务体内 (1)目标 mutation (2)`DELETE FROM pending_confirmations WHERE session_id=? AND receipt_id=?`(changes==1 断言)(3)事件+outbox** -> 事务成功后删内存+notifyResolve+audit;事务失败=释放预占,pending 完整保留(用户可重答)。非 Focus 域(dispatch 等)消费端为各自既有事务,DELETE 并入同层(dispatch 消费函数内);无库事务的轻终局(reject/dismiss/withdraw/expired)仍是独立 DELETE(无 mutation,无原子性需求)。
- **空间删除 effect 漂移(A-新5 后半)**:删除确认 payload 冻结 `affectedFocusIds` exact set;消费事务内重查现势集合,不等=stale("这个空间里的东西变了,我重新确认一遍")。
- digest 组成/分级/expiry/nonce 同 v0.3.2 §6。

## 7. 多宿主(不变)

## 8. 实施批次

| 批 | 内容 | 门 |
|---|---|---|
| 批 0 | 术语人话化 | [done] f2753ed |
| 批 1 | §5.1+§5.3+§5.4+§6(含 6.1 同事务)+§4.2 表+§2 spaces(v20 additive) | 三审 fixed+本版闭合,先行施工 |
| 批 1.5 | v21 events rebuild(+needs 回填)+v22 focuses rebuild | 同上 |
| 批 2 | 看板+空间+新建+attention API(§5.2 含停机过滤/binding 条件/blocked 分支/去重) | 同上 |
| 批 3 | §3 全(v23 obligations rebuild+v24 artifacts rebuild)+航迹+任务弹窗 | **四审确认后派** |
| 批 4 | 记录流 | 随批 3 后 |
| 迁移轨 | §1 三窗 | 独立 |

## 9. 迁移纪律

### 9.1 controlled rebuild 通用程序(内联定稿,六审 C-3;v21 已按此落库 ce0e381,v23/v24 照抄)

(1)BEGIN IMMEDIATE(迁移框架事务内则顺势);(2)CREATE 新表(目标结构逐列保真);(3)INSERT SELECT 全量;(4)断言前后 COUNT(*) 相等+内容 checksum(关键列逐行 hash 聚合)一致;(5)DROP 旧表+RENAME 换位;(6)重建全部 immutable triggers 与索引;(7)`PRAGMA foreign_key_check` 零行断言;(8)旧库 fixture 回归测试;任一步失败=ROLLBACK 原表原位。v21 特有:去 type CHECK(校验上移 contracts zod 枚举+写入口断言)+UNIQUE(focus_id,seq)+needs 存量回填(decision->decision/answer->input/其余->unknown,仅 human 未结行)。

### 9.2 v22 focuses rebuild(lifecycle 六值;已落库 ce0e381)
### 9.3 版本表统一(B2/(7))

v20=批 1 additive(pending/needs 列/artifacts/spaces/task_context)|v21=events rebuild+needs 回填|v22=focuses rebuild|**v23=obligations rebuild(批 3:lane_id 复合 FK+created_from_event)**|**v24=artifacts rebuild(批 3:created_from_event 复合 FK)**|sessions rebuild=迁移轨窗 2(版本号届时顺延)。每 rebuild 按 §9.1 通用程序(count+checksum+触发器索引重建+fk_check+fixture+回滚)。

## 10. invariant matrix 增补

- **space_* 事件域裁定(A15 尾)**:space 生命周期与归组=**audit 域**(不进 focus_events——它非单 Focus 语义;matrix 对应行 event 列改 audit:space.created/renamed/deleted/assigned);
- **renderer 全表+payload 冻结字段(B-2/B-3 定稿;渲染只读事件 payload 不回表——事件时快照原则)**:
  | 事件 | renderer 文案 | payload 必含(冻结) |
  |---|---|---|
  | lane_split | 拆出线:{titles} | laneIds[],titles[],parentLaneIds[],obligationIds[],baseline |
  | lane_retired | 收起线「{title}」,结掉 {n} 件 | laneId,title,resolvedIds[],baseline |
  | redo_from | 从第 {anchorSeq} 步重走,作废 {n} 件 | laneId,anchorSeq,supersededIds[],baseline |
  | dependency_set | 「{dep}」排队等「{pre}」 | depId,depTitle,preId,preTitle |
  | dependency_woken | 「{pre}」落定,「{dep}」开跑 | depId,depTitle,preId,preTitle |
  | dependency_blocked | 前置已终止:「{dep}」搁置 | depId,depTitle,preId,preTitle,preResolution |
  | lifecycle_changed | {from 人话}->{to 人话}({reason}) | from,to,reason |
  | focus_forked | 以「{sourceTitle}」为底续开 | sourceId,sourceTitle,newId |
  | artifact_linked | 登记产物「{title}」 | artifactId,kind,role,title |
  | artifact_realized | 产物「{title}」交付 | artifactId,title,fromRole,toRole,oldRefJson,newRefJson |
  | obligation_opened | 记下一件事:{title} | obligationId,title,owner,kind,dedupeKey,laneId? |
  | obligation_resolved | 办结:{title}({resolution 人话}) | obligationId,title,resolution |
  | space_* | (audit 域,不进航迹) | — |
- lane tree 正反例(§3.5)与 redo exact set 正反例(勾选集与现势漂移=stale)补入;
- archive/reopen 行补停机矩阵断言(archived 后 attention 零条目/reopen 后重现);
- **换锚原子性负例(六审)**:switch/确认换锚/undo 三路径各断言——目标 focus 为 archived/closed/abandoned 时事务拒(focus_not_active),且**旧 activation 仍 active、sessions.primary_focus_id 与 focus_anchor_revision 原样未动**(逐字段断言);undo 目标归档=当前 activation 不动+播报如实。

## 11. 待拍板

P-a/P-c/P-d/P-e 同 v0.3.2(P-d 已按 unknown 实施为默认建议)。

## 12. 待设计清单

原清单+**P-b(Focus Contract 开源)**+session-project detach+artifact version/invalidation 深化+**义务级主动通知(四审 A-2:无 durable 承载,批 3 零通知;outbox/dedupe/lifecycle send-fence 一并设计)**。
